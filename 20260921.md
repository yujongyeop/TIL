> 💡 **쿠버네티스(K8s) 오브젝트란?**
>
> K8s를 구성하는 단위로, 기본적인 구성단위인 기본(Basic) 오브젝트와 기본 오브젝트를 관리하고 추가적인 기능을 가진 컨트롤러(Controller) 오브젝트로 이루어져 있다.

# 기본 오브젝트(Basic Object)

- K8s에 의해서 배포 및 관리되는 가장 기본적인 오브젝트이다.
- `Pod`, `Service`, `Volume`, `Namespace` 4가지가 있다.

## 1. Pod

- K8s에서 **생성/배포할 수 있는 가장 작은 단위**이다.
- 1개 이상의 컨테이너로 구성되어 있다.
    - 보통 1 파드 = 1 컨테이너로 구성한다.
- 같은 파드 내 컨테이너들은 네트워크(IP)와 스토리지를 공유한다.
    - 즉, 파드 내 컨테이너는 localhost를 사용해 서로 통신할 수 있다.
- 파드는 **일회성(Ephemeral)**이기 때문에 고정 IP가 없고, 죽으면 재생성될 뿐 복구되지 않는다.

## 2. Service

- 파드 집합에 접근하기 위한 **고정된 네트워크 엔드포인트**이다.
- 파드는 재생성될 때마다 IP가 바뀌기 때문에 이를 안정적으로 묶어주는 역할을 Service가 한다.
- 라벨 셀렉터(`Label Selector`)로 어떤 파드들을 묶을지 결정한다.
    - Service는 IP가 아니라 **라벨(label)**로 파드를 찾는다.

### 2-1. Service 타입

- `ClusterIP`
    - 기본값이며, 클러스터 내부에서만 접근 가능한 고정 IP를 부여한다.
    - 내부 마이크로 서비스 간 통신할 때 사용한다.
- `NodePort`
    - 각 노드의 특정 포트로 외부에 노출시킨다.
    - 간단한 외부 접근(개발/테스트용)할 때 사용한다.
- `LoadBalancer`
    - 클라우드 제공업체의 로드밸런서를 자동으로 생성한다.
    - 프로덕션 환경을 외부 노출시킬 때 사용한다.
- `ExternalName`
    - 클러스터 외부 DNS 이름으로 리다이렉트한다.
    - 외부 서비스(DB 등)를 연결하는 용도이다.

## 3. Volume

- 파드 내 컨테이너가 사용할 수 있는 **저장 공간**이다.
- 컨테이너가 재시작되어도 데이터가 유지되도록 한다.
    - 단, 파드 자체가 사라지면 `emptyDir` 등 일부 타입은 함께 사라진다.

### 3-1. Volume 타입

- `emptyDir`
    - **파드가 생성될 때 함께 생성되는 빈 디렉토리이다.**
    - 같은 파드 안의 여러 컨테이너가 데이터를 공유할 때 사용한다.
    - 파드가 삭제되면 데이터도 함께 사라진다. (단, 컨테이너가 재시작되는 것만으로는 사라지지 않는다.)
    - 저장 위치: 노드의 디스크(또는 메모리)에 저장된다.
    - 용도
        - 임시 캐시, 컨테이너 간 스크래치를 공유할 때 적합하다.
        - 정렬/처리 중인 임시 파일을 공유할 때 적합하다.
- `hostPath`
    - **워커 노드의 실제 파일시스템 경로를 파드에 직접 마운트**한다.
    - 파드가 삭제돼도 데이터는 **노드에 그대로 남는다**.
        - 단, 파드가 다른 노드로 재스케줄링되면 그 데이터에 접근할 수 없다.
    - 용도
        - 노드의 시스템 파일 접근이 필요한 경우 적합하다.
        - 보안상 위험할 수 있어 프로덕션에서는 신중하게 사용해야 한다.
- `Persistent Volume`
    - **클러스터 수준에서 관리되는 스토리지 리소스**이다.
        - 노드/파드 생명주기와 독립적이다.
    - 파드나 노드가 사라져도 **데이터는 계속 유지된다.**
    - 보통 `PersistentVolumeClaim(PVC)`을 통해 사용 요청을 하고, 이를 PV와 연결한다.
        - Pod 입장에서는 PVC를 볼륨으로 사용한다.
    - 용도
        - 데이터베이스처럼 영구적으로 보존되어야 하는 데이터에 적합하다.
        - 클라우드 스토리지와 연동할 때 적합하다.
- etc

## 4. Namespace

- 하나의 클러스터 안에서 **리소스를 논리적으로 분리하는 가상 클러스터** 개념이다.
- 팀별, 환경별(dev/qa/prod)로 리소스를 나눌 때 사용한다.
- 기본적으로 `default`, `kube-system`, `kube-public` 등이 존재한다.

### 4-1. Namespace 타입

- `default`
    - **네임스페이스를 따로 지정하지 않았을 때** 리소스가 생성되는 곳이다.
    - 일반 사용자가 직접 리소스를 만들 때 기본적으로 사용하는 공간이다.

  > ⚠️ 실무에서 사용 시 `default` 네임스페이스를 사용하지 않고, 프로젝트/팀/환경별로 별도 네임스페이스를 만들어서 사용하는 것을 권장한다.

- `kube-system`
    - **쿠버네티스 시스템 자체가 사용하는 컴포넌트**들이 위치하는 곳이다.
    - `kube-apiserver`, `kube-scheduler`, `kube-controller-manager`, `coredns`, `kube-proxy` 등의 파드가 이를 사용한다.
    - 사용자가 직접 여기에 리소스를 만들거나 건드리는 건 권장하지 않는다.
- `kube-public`
    - **모든 사용자가 읽을 수 있는** 공개 네임스페이스
    - 클러스터 전체에 공개해야 하는 정보를 저장할 때 사용한다. (Ex. 클러스터 정보, ConfigMap 등등)
    - 실제로는 거의 비어있거나 특수한 용도로만 사용되고, 일반적인 개발에서는 잘 쓰이지 않는다.
- `kube-node-lease`
    - 최신 버전에서는 기본적으로 생성된다.
    - 각 노드의 `heartbeat`(생존 신호) 정보를 관리하는 `Lease` 오브젝트들이 위치한다.
    - 노드가 살아있는지 빠르게 판단하는 데 사용한다. (컨트롤러 매니저 부하 감소를 목적으로 한다.)