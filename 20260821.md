# 람다식

> 💡 람다식은 JDK 1.8에 추가되었다.
> 람다식의 추가로 자바는 객체지향언어인 동시에 함수형 언어가 되었다.

## 람다식이란?

- 메소드를 하나의 식으로 표현한 것
- 이를 통해 함수를 간략하면서도 명확한 식으로 표현할 수 있음
- 메서드의 이름과 반환값이 없어지므로, 익명 함수라고도 함
    - 클래스 선언 및 객체 생성 없이 바로 사용 가능
### 번외. 메소드와 함수의 차이

- 메소드는 특정 클래스에 반드시 속해야 함
- 함수는 특정 클래스에 속할 필요 없이 자유롭게 사용
## 람다식 작성

- 이름과 반환타입을 두지 않음
- 매개변수 선언부와 몸통 사이에 `->` 를 사용
```java
(a,b) -> {}
```

- 끝에 `;` 를 붙이지 않음
- 매개변수가 하나이면 괄호 생략 가능
    - 매개변수의 타입 있으면 불가
- `{}` 안의 문장이 하나이면 `{}` 생략 가능
    - return문일 경우 불가
## 함수형 인터페이스

- 람다식도 실제로는 익명 객체
- 함수형 인터페이스는 (하나의 메소드가 선언된 인터페이스로) 람다식을 다루기 위한 인터페이스
- 함수형 인터페이스를 매개 변수로 지정하면 람다식을 넘겨줘야 함
    - 람다식을 참조변수로 다룰 수 있다는 것은 메소드를 통해 람다식을 주고받을 수 있다는 의미
### 함수형 인터페이스의 제약

- 오직 하나의 추상 메소드만 정의되어 있어야 함
    - static 메소드와 default 메소드의 개수에는 제약이 없음
- `@FunctionalInterface` 를 붙이면, 컴파일러가 함수형 인터페이스를 올바르게 정의 했는지 확인
## 람다식의 타입과 형변환

- 함수형 인터페이스로 람다식을 참조할 수 있는 것일 뿐, 람다식의 타입이 함수형 인터페이스의 타입과 일치하는 것은 아님
    - 람다식은 익명 객체(=타입이 없음)
- 따라서 대입 연산자의 양변의 타입을 일치시키기 위해 **형변환(type-casting)이 필요**(생략 가능)
```java
MyFunction f = (MyFunction) (()->{});
```

- 람다식도 객체지만 Object타입으로 형변환은 불가하고, 함수형 인터페이스로만 형변환 가능
## 외부 변수를 참조하는 람다식

- 람다식 내에서 참조하는 지역변수는 `final` 이 붙지 않았어도 상수로 간주
- 외부 지역변수와 같은 이름의 람다식 매개변수는 허용되지 않음
```java
void method(int i){
	int val = 30;
	MyFunction f = (i) -> { // 에러, val을 사용해도 마찬가지
		// 문장들
	}
}
```