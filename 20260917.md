> 자바에서 "클래스 종류"는 보통 세가지 기준으로 나눈다.

# 선언 위치 기준

- 탑레벨 클래스(top-level class)
    - 파일 최상단에 선언되는 일반 클래스이다.
    - 한 파일에 `public` 클래스는 하나만 올 수 있고, 파일명과 이름이 같아야 한다.
    - 우리가 가장많이 만드는 클래스이다.
- 중첩 클래스(nested class)
    - 클래스 안에 선언되는 클래소로 네 가지로 나뉜다.
    - 바깥 인스턴스 없이 생성 가능한 **정적 중첩 클래스**
    - 바깥 인스턴스에 종속된 **내부 클래스**
    - 메소드 안에서만 유효한 **지역 클래스**
    - 선언과 동시에 일회성으로 사용되는 **익명 클래스**

```java
class Outer {
    static class StaticNested { }   // 정적 중첩 클래스: 바깥 인스턴스 없이 생성 가능
    class Inner { }                 // (비정적) 내부 클래스: 바깥 인스턴스에 종속

    void method() {
        class Local { }             // 지역 클래스: 메서드 안에서만 유효
        Runnable r = new Runnable() {   // 익명 클래스: 선언과 동시에 일회성 생성
            public void run() { }
        };
    }
}
```

# 제어자 기준

| 종류 | 특징 |
| --- | --- |
| `abstract` 클래스 | • 추상 클래스<br>• 직접 인스턴스화 불가<br>• 상속해서 구현 |
| `final` 클래스 | • 상속 금지(Ex. String, Integer 등) |
| `sealed` 클래스 | • 상속 가능한 하위 클래스를 `permits`로 제한 |
| `public` 클래스 | • 접근 범위 차이<br>• 제어자가 없으면 같은 패키지에서만 접근 |

# 클래스와 비슷한 별도 타입

- `interface`
    - 추상 메소드 모음
    - Java 8부터 `default`, `static` 메소드도 선언 가능
- `enum`
    - 고정된 상수 집합
    - 내부적으로는 클래스로 컴파일됨
- `record`
    - 불변 데이터 전용 클래스
    - `생성자`, `equals`, `hashCode`, `toString`이 자동 생성

# `final` 클래스

- `final` 클래스는 **상속을 금지하는 클래스**이다.
- 다른 클래스의 `extends` 대상이 될 수 없다.

```java
public final class Money { }
class Won extends Money { } // 컴파일 에러
```

- 상속을 막는 이유
    - 불변성 보장
        - `String` 같은 클래스는 `equals`와 `hashCode`가 항상 정직하다는 전제로 HashMap 키, 보안 검증, 클래스 로딩 등 많은 곳에서 쓰인다.
        - 만약 상속을 열어두어 하위 클래스가 그 전제를 깨버리면 시스템 전체의 가정이 무너진다.

          ```java
          // 만약 이렇게 하면 EvilString을 객체들이 equals 연산을 할 때 무조건 참을 반환하는 문제 발생
          class EvilString extends String {
              @Override public boolean equals(Object o) { return true; }
          }
          ```

    - 상속을 감당할 자신이 없을 때

      > 상속을 위해 설계하고 문서화하라. 그럴 수 없다면 상속을 금지하라. "이펙티브 자바"

        - 상속을 허용하는 순간, 어떤 메소드가 내부에서 어떤 메소드를 호출하는지까지 공개 API가 된다.

          ```java
          // 문제점: AddAll이 내부적으로 add를 쓴다
          class Parent {
              void addAll(List<String> items) {
                  for (String s : items) add(s);   // 내부에서 add 호출
              }
              void add(String s) { /* ... */ }
          }
    
          class Child extends Parent {
              int count = 0;
              @Override void add(String s) { count++; super.add(s); }
              @Override void addAll(List<String> items) {
                  count += items.size();
                  super.addAll(items);    // super가 또 add를 부름 → 중복 카운트
              }
          }
          ```

## `final` 클래스 특징

1. `final abstract`는 모순이라 컴파일 에러가 발생한다.
2. `final` 클래스의 모든 메소드는 자동으로 `final` 취급된다.(생략 가능)
3. **`enum`과 `record`는 암묵적으로 `final`** 이다.
4. 클래스가 `final`이어도 필드는 변경 가능하다.
    1. `final` 클래스 ≠ 불변 클래스
    2. 불변까지 원하면 필드도 `final`로 막아야 한다.