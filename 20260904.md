# 기본으로 등록되는 Advisor

## 1. `ToolCallingAdvisor`

- 툴 호출 루프를 담당한다.
- `DefaultChatClient`가 자동으로 등록한다.
- 모델이 툴 호출 없는 응답을 낼 때까지 요청/응답 루프를 돌린다.
- 우선 순위: `HIGHEST_PRECEDENCE + 300`

## 2. `ChatModelCallAdvisor`

- **체인 맨 끝에서 실제로 `ChatModel`을 호출하는 종단 어드바이저이다.**
- 종단 어드바이저이기 때문에 **`nextCall()` 메소드를 호출하지 않는다.**
- `ChatClientResponse`(응답)를 반환한다.
    - 요청 객체의 context를 응답 객체로 옮긴다.
- 우선 순위: `LOWEST_PRECEDENCE` (가장 낮은 순위)

# 내장 어드바이저

- Spring AI가 제공하는 Advisor로 구현체가 있지만, 사용하려면 개발자가 체인에 직접 등록해야 한다.

## 대화 메모리

- `MessageChatMemoryAdvisor`
    - 이전 대화를 메시지 목록 형태로 프롬프트 앞에 붙인다.
    - `[user] … [assistant] …` 구조가 그대로 유지되어 모델이 대화 이력으로 인식한다.
    - `conversationId`가 필수이다.
    - 우선 순위: `HIGHEST_PRECEDENCE + 200` (ToolCalling 바깥에 있다.)
- `VectorStoreChatMemoryAdvisor`
    - 장기 기억용으로 대화를 벡터 스토어에 넣어두고 현재 질문과 의미적으로 가까운 과거 대화만 꺼내온다.
    - 이력이 아주 길어 전부 넣을 수 없을 때 사용한다.
    - `spring-ai-vector-store-advisor` 의존성이 필요하다.

## RAG

- `QuestionAnswerAdvisor`
    - 가장 단순한 RAG이다.
    - 사용자 질문으로 벡터 스토어를 유사도 검색하고, 결과 문서를 사용자 메시지 뒤에 컨텍스트로 붙인다.

        ```java
        QuestionAnswerAdvisor.builder(vectorStore)
            .searchRequest(SearchRequest.builder().similarityThreshold(0.8).topK(6).build())
            .build();
        ```

    - 런타임에 `QuestionAnswerAdvisor.FILTER_EXPRESSION` 파라미터로 메타데이터 필터를 바꿀 수 있다.
    - 프롬프트를 커스텀하려면 템플릿에 `query`와 `question_answer_context` 두 플레이스홀더가 반드시 있어야 한다.
- `RetrievalAugmentationAdvisor`
    - 조립식 RAG이다.
    - `spring-ai-rag` 의존성이 필요하다.
    - 질의 변형, 질의 확장, 검색, 병합, 후처리, 증강 등을 처리할 수 있다.
    - **검색 결과가 비면 모델에게 답하지 말라고 지시한다.**
        - 답을 못 받는다며 헤매는 원인이 대개 이것과 너무 높은 `similarityThreshold` 때문이다.
- 선택의 기준
    - 검색해서 붙이면 끝일 경우 `QuestionAnswerAdvisor`
    - 질의 재작성이나 재랭킹이 필요하면 `RetrievalAugmentationAdvisor`

## `StructuredOutputValidationAdvisor`

- 구조화 출력 검증을 담당한다.
- `.entity(클래스명.class)` 같은 걸 썼을 때 모델 응답이 스키마를 안 지키면 오류를 알려주면서 다시 물어본다.
- 자가 교정 루프이다.
- 우선 순위: `LOWEST_PRECEDENCE`
    - 모델 호출 직전에 붙어야 재시도 구간이 최소가 될 수 있기 때문에 우선 순위가 낮다.
- `maxRepeatAttempts`로 횟수를 제한한다.

## `SafeGuardAdvisor`

- 유해 콘텐츠 차단을 담당한다.
- 금칙어 목록에 걸리면 `nextCall()` 메소드를 호출하지 않고, `failureResponse`를 그대로 돌려준다.
- LLM 호출이 아예 일어나지 않는다.
- 단순 단어 매칭이기 때문에 실제 방어가 필요하면 모더레이션 모델을 붙이는 게 맞다.
    - 공식 문서에서도 차단하는 어드바이저를 만드는 예시로써 설명되어 있다.

## `SimpleLoggerAdvisor`

- 로깅을 담당한다.
- 요청과 응답을 로그로 남기고 아무것도 바꾸지 않는다.
- `logging.level.org.springframework.ai.chat.client.advisor=DEBUG`를 켜야 보인다.
- 요청/응답 포맷 함수와 order를 생성자로 넘겨 커스텀할 수 있다.
- 우선 순위를 `ToolCallingAdvisor`보다 크게 주면 루프 회차별 요청을 전부 볼 수 있고, 작게 주면 최종 결과만 본다.

---

#### 클로드가 정리해준 우선 순위

```
HIGHEST ──────────────────────────────────────────► LOWEST
 SafeGuard   Memory(+200)   ToolCalling(+300)   StructuredOutput   ChatModelCall
                                    └─ 이 뒤는 매 iteration 실행 ─┘
```