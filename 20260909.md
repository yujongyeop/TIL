# 하네스 엔지니어링 8원칙
# 원칙 1. 사람이 조종하고, 에이전트가 실행한다

- 엔지니어는 코드를 작성하는 것이 아니라 환경 설계, 의도 명시, 피드백 루프 구축을 해야 한다.
- 사람은 항상 루프에 남아 업무를 우선시하고, 사용자 피드백을 수용 가능한 기준으로 바꾸며, 결과를 검증한다.

# 원칙 2. 애플리케이션을 에이전트가 읽게 하라

- **에이전트가 자기가 만든 것을 직접 실행하고 관찰하고 검증할 수 있도록, 앱의 동작을 에이전트가 읽을 수 있는 형태로 노출한다.**
- 현재 병목은 사람이 일일이 동작 여부와 코드를 검수하는 데서 발생한다.
- 에이전트가 직접 앱을 띄우고, MCP 등을 통해 에이전트가 직접 검증하고 고치는 일을 하게 한다.
- **검증을 할 수 없으면 고친 게 아니다.**

# 원칙 3. 프로젝트 저장소 지식을 기록 시스템으로 삼아라

- **"왜 이렇게 했는가"를 사람의 기억과 채팅에서 꺼내, 버전 관리되는 저장소 아티팩트로 옮긴다.**
- 코드는 결정의 결과만 담고 이유는 담지 않는다. 사람 팀은 그 공백을 슬랙 스레드와 선임의 머릿속으로 메우지만, 에이전트에게는 그 채널이 없다.
- 맥락이 없으면 에이전트는 멈추는 게 아니라 **추정한다.** 매번 다른 가정 위에서 코드를 짜기 때문에, 한 사람이 쓴 것 같지 않은 코드베이스가 된다.
- 백과사전이 아니라 **지도**를 만든다. 짧은 `AGENTS.md`(100줄 내외)가 진입점이 되고, 상세는 구조화된 `docs/`(설계 문서·실행 계획·레퍼런스)에 저장한다.
- **읽히지 않는 문서는 없는 문서다.** 분량이 아니라 찾을 수 있는지가 기준이다.

# 원칙 4. 에이전트 가독성에 최적화하라

- **코드 구조의 심사 기준을 "숙련자가 우아하다고 느끼는가"에서 "맥락 없이 들어온 에이전트가 길을 잃지 않는가"로 바꾼다.**
- 사람이 보기 좋다고 여기는 추상화 — 영리한 메타프로그래밍, 암묵적 규약, 마법 같은 자동 연결 — 은 "이 팀의 관행을 이미 아는 사람"을 전제한다.
- 에이전트는 매번 신입 상태이다. 전제를 공유하지 않은 **에이전트**는 진입점에서 길을 잃고 로컬 패턴 매칭에 빠진다.
- 작은 진입점, 명시적 경계, 일관된 모듈 패턴, 계층이 분명한 구조를 쓴다. 명시적이고 반복적이고 조금 지루한 코드가 에이전트에게는 더 낫다.

# 원칙 5. 아키텍처와 취향을 기계적으로 강제하라

- **규칙이 실재하는지를 "적혀 있는가"가 아니라 "어기면 빌드가 깨지는가"로 판별한다.**
- 사람 팀에서는 문서에만 있는 규칙도 코드 리뷰라는 인간 필터로 어느 정도 지켜진다. 에이전트가 밤새 돌면 그 필터가 없다.
- 강제되지 않는 규칙은 위반되는 게 아니라 **조용히 잊힌다.**
- 맞춤 린터, 구조적 테스트, CI로 위반을 빌드 단계에서 차단한다. 문서는 사람이 보는 지도고, 강제는 기계가 할 일이다.
- 진짜 값어치는 드리프트 방지가 아니라 **속도**다. 기계가 막아주는 게 늘수록 사람이 리뷰에서 봐야 할 게 줄고, 그래야 원칙 6이 성립한다.

# 원칙 6. 처리량에 맞춰 병합 철학을 바꿔라

- **리뷰라는 자원이 유한함을 인정하고, 그 자원을 판단이 필요한 곳에만 쓰도록 병합 절차를 다시 설계한다.**
- 짧은 PR·짧은 수명·빠른 병합을 기본으로 하고, 자동 검증과 에이전트 검토자 루프를 앞단에 둔다. 사람 승인은 판단이 필요할 때만 남긴다.
- 이 원칙은 오용되기 쉽다. "처리량이 올랐으니 게이트를 낮추자"로 미끄러지면 원칙이 아니라 변명이 된다.
- **게이트를 낮추는 게 아니라 사람에서 기계로 옮기는 것이다.** 옮길 기계가 아직 없다면 이 원칙을 적용할 때가 아니다.

# 원칙 7. 자율 루프를 시스템으로 구축하라

- **에이전트가 일하기 위해 사람이 버튼을 눌러야 하는 지점을 하나씩 없앤다.**
- 매번 사람이 트리거해야 한다면, 에이전트가 아무리 빨라도 전체 처리량의 상한은 사람이 하루에 누를 수 있는 버튼 수다.
- 자율성이 사람의 근무 시간에 묶이면 야간과 주말에는 작업을 하지 않는다. 반복 작업은 백그라운드 태스크와 루프(스캔 → 수정 PR)로 만든다.
- 자율 수준은 스위치가 아니라 **다이얼**이다. **다이얼을 어디까지 돌려도 되는지는 원칙 2와 5가 결정한다.** 관측과 강제 없이 돌리면 사고를 가속하는 장치가 된다.

# 원칙 8. 엔트로피를 가비지 컬렉션하라

- **무질서가 생성 속도에 비례해 늘어난다는 걸 전제로, 정리를 이벤트가 아니라 상시 프로세스로 만든다.**
- 사람이 쓰는 코드베이스에서도 죽은 코드·명명 표류·문서 불일치는 쌓인다. 다만 속도가 타이핑 속도에 묶여 있어 주기적인 리팩토링으로 문제를 잡을 수 있다.
- AI로 인해 코드 생성이 빨라지면 더 이상 주기적인 리팩토링만으로는 문제를 잡을 수 없다.
- "황금 원칙" 문서 + 품질 등급 + 정기 정리 PR로 회수를 자동화한다. GC는 쓰레기를 안 만드는 게 아니라 **생기는 걸 전제로 회수를 자동화**하는 개념이기 때문이다.
- **정리할 양이 계속 늘어난다면 그건 8의 문제가 아니다.** 보통 원칙 4나 5가 약한 것이다.

---

> 💡 위에서 언급한 8원칙은 실제로는 조합해서 쓰는 네 가지 패턴으로 묶는다.
>
> 1. SoT
> 2. 검증 게이트와 모델 다양성
> 3. 결정론적 산출물
> 4. 멀티 에이전트 오케스트레이션