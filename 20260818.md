# SQL이란? (with PL/SQL)

## SQL의 정의

> 나무위키: SQL is set-based, declarative query language, not an imperative(명령형) language such as C or BASIC

> 💡 **구조적(Structured)**이고 **집합적(Set-based)**이고 **선언적(Declarative)**인 질의 언어다.
> 결과집합을 구조적, 집합적으로 선언하지만, 그 결과집합을 만드는 과정은 절차적일 수밖에 없다.

## PL/SQL은?

- PL/SQL(or T-SQL)은 절차적(procedural) 프로그래밍 기능을 구현할 수 있는 확장 언어 개념

# SQL 최적화는 뭔가?

- DB는 결과 집합을 만들어 내기 위해서 **프로시저**가 필요
- 이런 프로시저를 만들어 내는 **DBMS 내부 엔진이 SQL 옵티마이저**
  - 옵티마이저가 사용자가 전달한 SQL문을 보고 프로시저(실행계획)를 작성

> 💡 DBMS 내부에서 **프로시저를 작성하고, 컴파일해서 실행 가능한 상태로 만드는 전 과정**을 SQL 최적화라고 한다.

# SQL 최적화 3단계

- SQL을 실행하기 전 최적화 과정은 크게 3단계로 세분화

## 1단계: SQL 파싱

- SQL Parser가 **SQL문을 파싱**하는 단계
- 처리 작업

| 작업명 | 작업 내용 |
| --- | --- |
| 파싱 트리 생성 | SQL문의 파싱 트리 생성 |
| Syntax 체크 | 문법적 오류가 없는지 확인<br>Ex. 키워드 순서, 사용할 수 없는 키워드 사용 등 |
| Semantic 체크 | 의미상 오류가 없는지 확인<br>Ex. 존재하지 않는 테이블/컬럼 사용, 권한 확인 등 |

## 2단계: SQL 최적화

- SQL 옵티마이저가 통계정보를 바탕으로 **다양한 실행 경로를 생성 및 비교 후 가장 효율적인 하나**를 선택
  - 시스템 및 오브젝트에 대한 통계정보를 사전에 수집
- 이 단계는 DB 성능을 결정하는 핵심 엔진

## 3단계: 로우(Row) 소스 생성

- SQL 옵티마이저가 선택한 실행경로를 실제 **실행 가능한 코드 또는 프로시저 형태로 포맷팅**
- 로우 소스 생성기에 의해서 생성

---

# Out of book

## Q. 왜 튜닝이 필요한가?

- SQL 옵티마이저는 통계 정보를 기반으로 최적의 비용으로 실행할 수 있는 실행 경로를 생성
- 성능 좋은 SQL 옵티마이저를 사용하는데 우리가 튜닝을 해야 하는 이유는 뭘까?

### A. SQL 옵티마이저도 실수를 한다.

- 가장 근본적인 문제는 **통계정보의 한계**이다.
- 옵티마이저는 데이터를 직접 읽고 판단하는 게 아니라, 사전에 수집된 **통계정보를 보고 비용을 추정**한다.
  - 테이블 크기, 컬럼 값 분포, 인덱스 상태 등

#### 문제점 1. 이 정보는 최신 상태가 아닐 수 있다.

- 통계 수집 시점 이후에 데이터가 대량으로 CUD가 이루어지면, 옵티마이저는 현재 데이터 상태를 반영하지 못한다.
- 예시: 통계 수집 시점에 테이블 레코드 크기는 10만 건이었는데, 1억 건의 데이터가 삽입된 상황

#### 문제점 2. 요약본을 정보로 사용한다. (정보 손실)

- 통계 정보는 데이터 전체가 아니라 샘플링된 요약 정보이다.
- 이로 인해 값 분포가 극단적으로 치우친 컬럼에서 히스토그램이 없거나 부정확하면 옵티마이저는 균등 분포를 가정한다. (실제 데이터: 불균등 분포, 옵티마이저 생각: 균등 분포)
- 예시: `status` 컬럼의 99%가 "완료", 1%가 "오류"인 상황