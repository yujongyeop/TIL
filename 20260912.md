# Redis

## Redis의 의미

- `Redis(REmote DIctionary Server)` 는 메로리에 데이터를 저장하는 Key-Value 저장소이다.
- 디스크가 아닌 메모리를 저장소로 사용하기 때문에 응답 시간이 매우 빠르다.(보통 1ms 미만)
- 캐시 목적으로 가장 많이 사용하며, 메시지 브로커와 데이터베이스로도 사용된다.

## Redis 자료 구조

- 단순 Key-Value가 뿐만 아니라 다양한 타입을 지원한다.
- `String` - 기본 자료형이며, 캐시나 카운터로 사용한다.
- `Hash` - 객체 형태의 필드-값 묶음, 사용자 프로필 같은 것에 사용한다.
- `List` - 양쪽 끝에서 넣고 뺄 수 있으며, 간단한 큐 역할을 한다.
- `Set` - 중복 없는 집합으로 교집합/합집합 연산이 가능하다.
- `SortedSet` - 점수로 정렬되는 집합으로 랭킹 보드의 표준 해법이다.
- `Stream` - 로그 형태의 append-only 구조이고, Kafka와 비슷한 소비자 그룹을 지원한다.
- 이 외에도 `Bitmap`, `HyperLogLog` 등을 지원한다.

## 주요 활용

- 캐싱과 세션 저장소로 가장 흔히 사용된다.
- 분산 락, Rate Limiting, 실시간 랭킹, Pub/Sub 기반 알림, 작업 큐 등에도 사용한다.

## 영속성

- 메모리 기반이지만 데이터를 디스크에 남길 수 있다.
- `RDB`: 특정 시점의 스냅샷을 통째로 저장한다. 빠르고 파일이 작지만 마지막 스냅샷 이후 데이터는 유실된다.
- `AOF` - 쓰기 명령을 로그로 기록한다. 유실이 적지만 파일이 크고 복구가 느리다.
- 실무에서는 둘을 함께 사용하는 경우가 많다.

### 실무 예시

1. `AOF` 파일 내부에서 `RDB` 포맷을 섞어서 쓰는 방식
    - `AOF` 의 최대 단점은 복구가 느리다는 것이다.
    - 이를 해결하려고 `AOF` 재작성(rewrite) 시점에 앞부분은 RDB 스냅샷, 뒷부분은 그 이후의 쓰기 명령으로 파일을 구성한다.
2. `AOF`는 복구용, `RDB`는 백업용으로 사용하는 방식
    1. `AOF`는 프로세스가 죽거나 서버가 재부팅 됐을 때 바로 살리기 위한 용도이다.
    2. `RDB`는 S3에 올리거나 다른 환경으로 옮기거나 특정 시점으로 복구할 때 사용한다.

## 확장과 고가용성

### 복제(Replication)

- 마스터-레플리카 구조로 읽기 분산 시킨다.
- 레플리카가 붙으면 마스터는 `BGSAVE` 로 RDB를 만들어 전송하고, 그 사이에 들어온 쓰기 명령은 버퍼에 모아뒀다가 이어서 보낸다.
- 이후로는 명령 스트림을 계속 흘려 보낸다.
- 해당 작업들은 비동기로 동작한다.

### Sentinel

- 복제만으로는 마스터가 죽었을 때 아무도 레플리카를 마스터로 승격 시켜주지 않는다.
- `Sentinel`은 마스터가 죽었을 때, 레플리카 중 하나를 마스터로 승격시키는 역할을 한다.

### Cluster

- 복제와 Sentinel은 가용성 문제를 풀지만, 데이터가 한 노드의 메모리에 다 들어가야 한다는 제약은 그대로 이다.
- Cluster는 데이터 자체를 쪼개서 여러 노드에 분산하여 저장하게 한다.