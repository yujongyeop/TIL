# 툴 전달 방식

* 툴 전달 방식은 **한 요청에만 추가하는 방식(`tools`)** 과 **모든 요청에 추가하는 방식(`defaultTools`)** 이 있다.

```java
// 단일 요청
chatClient.prompt(...).tools(new WeatherTools(), currentWeather).call();

// 모든 요청
ChatClient client = ChatClient.builder(chatModel)
        .defaultTools(new WeatherTools(), currentWeather)
        .build();
```

* `tools`와 `defaultTools` 모두 이종이다.

> **💡 이종(heterogeneous)이란?**
>
> 한 종류의 타입만 받는 게 아니라 서로 다른 종류를 섞어서 받는 것을 말한다.
> 위 메소드들은 `@Tool`이 붙은 `POJO`, `ToolCallback`, `ToolCallbackProvider`를 받는다.

* 호출별 `.tools()`는 기본값을 대체하지 않고 추가한다.
    * 최종 툴 목록은 `defaultTools`와 `tools`를 합친 것이다.

> **⛔ `defaultTools` 등록 시 유의사항**
>
> 기본 툴은 같은 빌더에서 만든 모든 요청에 공유되므로 편리하지만 부주의하면 위험하다.
> 위험 등급이 높거나 파괴적인 툴은 기본값이 아니라 호출별로 추가하라고 공식 문서에 명시되어 있다.
> 위험 등급이 높거나 파괴적인 툴: 특정 행동을 수행하거나 데이터 상태를 조작하는 행위(Ex. 회원 정보 삭제)

# 루프

* `.call()`이나 `.stream()`을 부르면 요청이 어드바이저 체인을 타고, `DefaultChatClient`가 자동 등록한 `ToolCallingAdvisor`가 툴 실행 생명주기를 관리한다.
* `ToolCallingAdvisor`는 재귀 어드바이저이다.
    * 루프의 매 반복마다 하위 어드바이저 체인에 재진입한다.
    * 구조화 출력 검증 재시도도 같은 메커니즘이다.
* `DefaultChatClient`는 `ToolAdvisor`가 정확히 하나만 존재하도록 강제하며, 두 번째를 등록하려 하면 명시적 에러로 실패한다.

## 동작 과정

1. 어드바이저가 등록된 모든 툴 정의를 포함해 요청을 모델에 보낸다.
2. 모델이 툴을 부를지 결정하고 응답한다.
3. 응답에 툴 호출이 있다면, `ToolCallingManager`에 넘겨 매칭되는 `ToolCallback`을 실행하고, 결과를 대화 기록에 추가 및 갱신된 기록을 모델에 재전송한다.
4. 모델이 툴 호출 없는 응답을 낼 때까지 2~3번 과정을 반복한다.

# `ChatMemory`와 루프의 관계

* `MessageChatMemoryAdvisor`의 기본 order는 `HIGHEST_PRECEDENCE + 200`으로, `ToolCallingAdvisor`보다 낮다.
    * 따라서, 메모리는 `ToolCallingAdvisor`보다 바깥에 놓인다.
    * 즉, 메모리는 툴 호출 전후로만 동작하기 때문에 툴 호출 루프에 대한 내용은 기록하지 않는다.
* 만약 order를 높여서 루프 안쪽으로 넣을 경우, `DefaultChatClient`가 루프 안쪽에 있는 `MemoryAdvisor`를 감지해 내부 기록을 알아서 비활성화한다.
    * Spring AI 2.1 버전부터 `spring-ai-session`에 등록된 `InMemoryChatMemoryRepository`, `RedisChatMemoryRepository`, `Neo4jChatMemoryRepository`는 루프 안쪽에서 사용 가능하게 될 예정이다.

# ToolContext - 모델에게 숨기는 데이터

* 사용자 ID, 비밀번호처럼 모델에 보내면 안 되는 데이터를 툴에 전달하는 통로이다.
* 모델에는 툴 정의만 가고 컨텍스트는 호출 시점에 툴에 직접 전달된다.

# Return Direct - 루프 끊기

* `returnDirect = true`면 결과가 모델을 거치지 않고 호출자에게 바로 간다.
* 툴 출력 자체가 최종 결과이고(Ex. RAG 검색) 추가 왕복이 지연만 늘릴 때 쓴다.
* 모델이 한 라운드에 여러 툴 호출을 요청하면 호출된 툴 전부가 `returnDirect = true`일 때만 동작한다.