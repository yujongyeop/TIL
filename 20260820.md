# Collection Framework란?

> 💡 데이터 그룹을 저장하는 클래스들을 표준화한 설계이다.
> 컬렉션 프레임워크를 활용하면 객체 지향적이고 재사용성이 높은 코드를 작성할 수 있다.

## Iterable 인터페이스

- 컬렉션 인터페이스들의 최상위 인터페이스
- iterator 객체를 관리하는 인터페이스

### 제공 메소드

1. `forEach(Consumer<? super T> action)`
    - 함수형 프로그래밍 전용 루프 메소드
2. `iterator()`
    - 컬렉션에서 iterator를 구현
3. `spliterator()`
    - 파이프라이닝 관련 메소드

> 💡 Map은 왜 iterator 관련 연산이 없나?
> 이는 Map 인터페이스는 Collection Framework에 속해 있지 않기 때문이다.
> 따라서, Map은 스트림(Stream)을 사용하거나 간접적으로 키를 Collection으로 반환하여 루프문으로 순회하는 방식을 이용한다.

## Collection 인터페이스

- List, Set, Queue가 상속하는 실질적인 최상위 컬렉션 타입
- 업캐스팅으로 다양한 종류의 컬렉션 자료형을 받아 자료의 CRUD 기능을 할 수 있게 하는 다형성을 제공

### 제공 메소드

1. `add(Object)`, `addAll(Collection)`
    - 객체 또는 Collection의 객체들을 Collection에 추가
2. `contains(Object)`, `containsAll(Collection)`
    - 객체(들)가 Collection에 포함되어 있는 지 확인
3. `remove(Object)`, `removeAll(Collection)`
    - 객체 또는 Collection에 포함된 객체들을 삭제
4. `retainAll(Collection)`
    - 지정된 Collection에 포함된 객체만 남기고 다른 객체들은 Collection에서 삭제
    - removeAll의 대칭 버전
5. `clear()`
    - Collection의 모든 객체 삭제
6. `equals(Object)`, `hashCode()`
    - 동일한 Collection인지 비교
    - Collection의 hash code 반환
7. `isEmpty()`, `size()`
    - Collection이 비어있는 지 확인
    - Collection에 저장된 객체 개수 반환
8. `toArray()`, `toArray(Object[])`
    - Collection에 저장된 객체를 객체배열로 반환
    - 지정된 배열에 Collection의 객체를 저장해서 반환
9. `iterator()`
    - Collection의 iterator를 얻어서 반환

> 💡 JDK 1.8부터는 함수형 프로그래밍을 위해 parallelStream, removeIf, stream, forEach 메소드가 추가되었다.

## Collection Framework의 상속계층도
[Collection 상속도](./img/Collection_상속도.png)