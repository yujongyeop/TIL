# `Optional<T>`이란?

* 제네릭 클래스로 T타입의 객체를 감싸는 `Warpper` 클래스이다.
* `Optional` 타입의 객체는 모든 타입의 참조변수를 담을 수 있다.
* `NPE` 가 발생하지 않도록 도와준다.

## `Optional` 생성

1. `Optional.empty()`

* 값이 `null`인 경우 이 메소드를 사용해 생성한다.

2. `Optional.of()`

* 값이 `null` 이 아닌 경우 사용한다.
* 값을 `null` 로 저장하려 하면 `NPE`가 발생한다.

3. `Optional.ofNullable()`

* 값이 `null`이 올 수도 있고 아닐 수도 있을 때 사용한다.

## `Optional` 지원 메소드

### 값 존재 여부 확인

* `isPresent()` , `isEmpty()`

### 값 꺼내기

* `get()` - 값이 없을 경우 `NoSuchElementException` 발생한다.
* `orElse()` - 값이 없을 경우 매개변수의 값을 반환한다.
* `orElseThrow()` - 값이 없을 경우 사용자가 지정한 예외를 던진다.

### 값이 있을 때 특정 동작 실행

* `ifPresent(consumer)`
* `ifPresentOrElse(consumer, runnable)`

### 변환 및 필터링

* `map(function)` - 함수를 적용한 결과를 `Optional`로 반환한다.
* `flatMap(function)` - 맵과 동일
* `filter(predicate)` - 필터링한 결과를 `Optional`로 반환한다.
* `or(supplier)` - 값이 있으면 자신 그대로, 비어있으면 `supplier`가 만든 다른 `Optional`을 반환한다.
* `stream()` - 0개 또는 1개 요소의 `Stream`을 반환한다.

## `Stream`에서 `Optional` 을 반환하는 메소드

* `findAny()`
* `findFirst()`
* `max(Comparator comparator)`
* `min(Comparator comparator)`
* `reduce(BinaryOperator<T> accumulator)`