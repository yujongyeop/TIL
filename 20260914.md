# 쿠버네티스(Kubernetes, k8s)

- 컨테이너를 자동으로 배포하고 관리해주는 오픈소스 플랫폼이다.
- 구글이 내부에서 쓰던 시스템을 기반으로 만들어 공개했고, 현재는 CNCF에서 관리하고 있다.

> 💡 **CNCF(Cloud Native Computing Foundation)**
>
> - 클라우드 네이티브 소프트웨어의 발전과 확산을 지원하는 비영리 단체이다.
> - 2015년 리눅스 재단이 설립하여 운영하고 있으며, 첫 프로젝트가 구글의 쿠버네티스이다.

# K8s 핵심 기능

## 1. 자가 치유

- 컨테이너가 죽으면 자동으로 재시작하거나 다른 서버에 다시 띄운다.
- 각 노드에는 kubelet이라는 에이전트가 돌면서 컨테이너 상태를 감시한다.
    - 프로세스가 종료되면 `restartPolicy`에 따라 즉시 재시작한다.
    - 계속 죽으면 재시작 간격을 점점 늘리는데(백-오프), 이 상태가 `CrashLoopBackOff`이다.
- 프로세스는 떠 있는데 응답을 못 하는 상태(데드락 or 메모리 누수)일때 프로브(`probe`)를 붙인다.
    - `livenessProbe`: 실패하면 컨테이너를 죽이고 재시작한다.
    - `readinessProbe`: 실패하면 트랙핀만 끊는다.(재시작은 안 함)
    - `startupProbe`: 기동이 오래 걸리는 앱을 위해, 기동 완료 전까지 위 두 프로브를 유예한다.
- kubelet이 컨트롤 플레인에 보내던 하트비트가 끊기면 약 40초 후 노드가 `NotReady`로 표시되고, 기본 5분 뒤 그 노드의 파드들이 축출된다.
    - `ReplicaSet`이 N개 있어야 하는데 N-1개만 있음을 감지하고 다른 노드에 새로 만든다.

> 💡 **중요한 유의점**
>
> 죽은 파드가 되살아나는 게 아니라 새 파드가 만들어진다. 이름도 IP도 바뀌고 컨테이너 안에 쓴 파일은 사라진다. 그래서 애플리케이션을 무상태(`stateless`)로 설계하고, 테이터는 볼륨이나 외부 DB에 두어야 한다.

## 2. 스케일링

- CPU 사용률 같은 지표에 따라 인스턴스 개수를 자동으로 늘리고 줄인다.
    - `metrics-server`가 수집한 CPU/메모리 사용률을 보고 15초마다 계산한다.
    - `목표 개수 = ceil(현재 개수 x (현재 사용률 / 목표 사용률))`
    - Ex. 현재 3개가 평균 90%를 쓰고 목표가 50%라면 → `ceil(3 x 1.8)` = 6개로 늘린다.
- `HPA(Horizontal Pod Autoscaler)`가 주력이며, 파드 개수를 늘리고 줄일 수 있다.
    - HPA의 CPU 기준은 **절대값이 아니라 `resource.requests` 대비 비율**이다.
    - `requests`를 설정하지 않으면 HPA가 아예 동작하지 않는다.
- 축소는 확장 때보다 보수적이다.
    - 기본적으로 5분간의 안정화 윈도우를 두고, 그 시간 동안의 최대 권장값을 쓴다.
    - 트래픽이 출렁일 때 파드가 늘었다 줄었다 요동치는 걸(flapping) 막기 위해서이다.
- 다른 종류
    - `VAP(수직)`: 파드 개수 대신 파드에 할당된 CPU/메모리 자체를 조정한다. HPA와 같은 지표로 동시에 쓰면 충돌한다.
    - `KEDA`: 큐 길이, Kafka 랙 같은 이벤트 기반 지표로 스케일링한다. 0까지 줄일 수도 있다.
    - `Cluster AutoScaler(CA)`: 파드를 놓을 노드가 부족하면 클라우드에 노드 자체를 추가 요청
        - `HPA`가 파드를 띄우라고 해도 물리적 공간이 없으면 `Pending`에 걸리며, `CA`는 그걸 보고 노드 개수를 조정한다.(노드를 늘리고 파드를 추가하여 `Pending`을 해제)

## 3. 로드 밸런싱

- 여러 인스턴스로 트래픽을 알아서 분산한다.
- 파드는 죽고 살아나면 IP가 계속 바뀌기 때문에, `Service`라는 고정된 주소를 앞에 둔다.
- Service는 레이블 셀렉터로 파드를 묶는다.
    - `app=web`인 파드를 전부 찾아 그 IP 목록을 `EndpointSlice`에 유지한다.
    - 파드가 생기거나 사라지면 이 목록은 자동 갱신된다.
    - 클러스터 내부 DNS에 `서비스이름.네임스페이스.svc.cluster.local`로 등록되어 다른 앱은 이 이름만 알면 된다.
- 실제 분산은 각 노드의 kube-proxy가 커널 레벨(iptables 또는 IPVS)에서 처리한다.
    - 중앙 프록시 서버를 거치지 않아 빠르지만, iptables 모드는 엄밀히 라운드로빈(RR)이 아니라 **확률 기반 무작위 선택**이다. 트래픽이 완벽히 균등하지 않을 수 있다.
    - IPVS 모드로 바꾸면 진짜 라운드로빈이나 최소 연결 같은 알고리즘을 쓸 수 있다.
- `Service` 타입 종류

  | 타입 | 용도 |
    | --- | --- |
  | ClusterIP | 클러스터 내부 전용(기본값) |
  | NodePort | 각 노드의 특정 포트를 외부에 개방 |
  | LoadBalancer | 클라우드 로드밸런서를 자동 생성 |

- Service는 L4(TCP/UDP)까지만 다루기 때문에 HTTP 경로 기반 라우팅이나 TLS 종료가 필요하면 **Ingress**(또는 최신 표준 Gateway API)를 쓴다.

## 4. 무중단 배포

- 새 버전을 조금씩 교체하면서 배포하고, 문제가 생기면 이전 버전으로 롤백한다.
- Deployment의 기본 전략인 롤링 업데이트는 `ReplicaSet(RS)` 두 개를 두고 저울질하듯 교체한다.
    - 구버전 `RS`를 줄이면서 신버전 `RS`를 늘린다.
- 속도는 두 값으로 조절한다.
    - `maxSurge`: 목표 개수를 얼마나 초과해도 되는지(기본값: 25%)
    - `maxUnavailable`: 동시에 몇 개까지 없어도 되는지(기본값: 25%)
        - 이 값을 `0`으로 두면 새 파드가 준비될 때까지 기존 파드를 절대 줄이 않아 **안전하지만, 여유 리소스가 필요하고 배포가 느려진다.**

> 💡 **롤링 업데이트를 켠다고 무중단이 되지 않는다.**
>
> `readinessProbe`와 `Graceful Shutdown`이 필요하다.
>
> `readinessProbe`가 없으면 쿠버네티스는 컨테이너가 시작되자마자 준비됐다고 판단하고 트래픽을 보낸다.(문제: 앱이 아직 DB 커넥션 풀도 못 만들었는데 요청이 들어오게 된다.
>
> 파드가 없을 때 쿠버네티스는 `SIGTERM`을 보내고 `terminationGracePeriodSeconds`(기본: 30초)만큼 기다린 뒤 `SIGKILL`로 강제 종료한다. 앱이 `SIGTERM`을 받아 처리 중인 요청을 마무리하도록 구현해야한다.

- 롤백은 이전 `RS`이 남아있어서 가능하다.
    - `kubectl rollout undo`로 되돌리고, 몇 개를 보관할지는 `revisionHistoryLimit`로 정한다.
- 더 정교한 방식이 필요할 경우 `Argo Rollouts`나 `Flagger` 같은 도구로 **카나리 배포나 블루/그린을** 구현한다.
    - 카리나 배포: 신버전에 트래픽 5%만 보내고 에러율을 보면서 점진 확대한다.

## 5. 선언적 관리

- YAML 파일에 설정을 적어두면, 쿠버네티스가 계속 그 상태를 맞춰준다.
- YAM에 원하는 상태를 적고 `kubectl apply`를 하면, 그 내용이 `etcd`에 저장된다.
    - 그리고 각 컨트롤러가 무한 루프를 돌며 아래를 반복한다.(이를 **조정 루프**라고 한다.)

      ```java
      1. 원하는 생태를 읽는다.
      2. 실제 상태를 관찰한다.
      3. 차이가 있으면 좁히는 행동을 한다.
      4. 1번으로 간다.
      ```

- YAML 파일이 곧 시스템 명세서라 Git에 넣고 버전 관리를 할 수 있다.
    - Git 저장소를 유일한 진실의 원천으로 두고. 에이전트가 클러스터를 계속 그 상태로 맞춘다.