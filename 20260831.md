# 제네릭 심화

## 1. 제네릭 메서드

- 클래스와 별개로 메서드 하나에만 타입 파라미터를 선언할 수 있다.
    - 반환 타입 앞에 `<T>` 붙이는 게 문법이다.

```java
public class Utils {
    // 여기서의 T는 Utils의 T와 무관한, 이 메서드만의 T를 의미한다.
    public static <T> List<T> toList(T... elements) {
        return Arrays.asList(elements);
    }

    public static <K, V> Map<V, K> invert(Map<K, V> map) {
        Map<V, K> result = new HashMap<>();
        map.forEach((k, v) -> result.put(v, k));
        return result;
    }
}
```

## 2. 제네릭 클래스

- 클래스 내부에서 사용하는 제네릭 타입은 `static` 메서드에 사용할 수 없다.

```java
class Box<T> {
    private T value;

    static void bad(T t) { }        // 컴파일 에러
    static <U> void good(U u) { }   // OK
}
```

## 3. 바운디드 타입 파라미터

- `T`에 아무거나 들어오면 `Object`의 메서드밖에 못 쓴다.
- `T` 타입의 상한을 걸면 해당 타입의 기능을 쓸 수 있다.

```java
public static <T extends Number> double sum(List<T> list) {
    double total = 0;
    for (T n : list) total += n.doubleValue();
    return total;
}
```

## 4. 다중 바운드

- `&`로 타입 파라미터에 제약을 여러 개 걸 수 있다. (`AND`를 의미하며, `OR`은 없다.)
- `,` - 타입 파라미터를 구분
- `&` - 한 파라미터의 바운드를 나열

```java
<T extends Number & Comparable<T> & Serializable>   // 파라미터 1개, 조건 3개
<T extends Number, U extends Comparable<U>>         // 파라미터 2개
```

### 제약

**1. 클래스는 하나만 지정할 수 있다.**

```java
<T extends Number>          // 가능
<T extends Number & Thread> // 클래스 두 개 상속 불가
```

**2. 인터페이스는 개수 제한이 없다.**

```java
<T extends Number & Comparable<T> & Serializable & Cloneable>
```

**3. 클래스는 반드시 맨 앞에 와야 한다.**

```java
<T extends Number & Comparable<T>>  // 가능
<T extends Comparable<T> & Number>  // 에러: interface expected here
```

**4. 타입 변수도 첫 자리만 가능하다.**

```java
<T, U extends T & Comparable<U>>   // T가 앞자리라 가능
<T, U extends Comparable<U> & T>   // T가 뒤에 오면 에러
```

> 💡 **타입 소거**
>
> 다중 바운드에서 타입 소거는 가장 왼쪽(첫 번째) 바운드로 치환된다.
>
> - `<T extends Number & Comparable<T>>` → T가 `Number`로 소거
> - `<T extends Comparable<T> & Serializable>` → T가 `Comparable`로 소거
>
> 첫 번째 바운드의 메서드는 캐스팅 없이 호출되지만,
> 두 번째 이후 바운드의 메서드는 컴파일러가 캐스팅을 삽입해서 호출한다.