# Stream 최종 연산과 쇼트 서킷

* 최종 연산은 스트림 파이프라인을 실제로 실행시키고 결과를 반환한다.
* 이 연산이 호출되는 순간 중간 연산들이 한꺼번에 처리되며, 사용된 스트림은 재사용 불가하다.

## 최종 연산 메소드

### 반복 소비

* `void forEach(Consumer)`
    * 각 요소에 대해 동작 수행(병렬 수행 시 순서 보장 X)
* `void forEachOrdered(Consumer)`
    * 원본 순서를 보장하며 동작 수행

### 집계/통계

* `long count()`
* `Optional<T> min(Comparator)`
* `Optional<T> max(Comparator)`
* `int(|long|double) sum()` - 기본형(primitive)스트림 전용
* `Optional<Double> average()` - 기본형(primitive)스트림 전용
* `XXSummaryStatistics summaryStatistics()` - 위 메소드 결과를 한 번에 반환

### ⭐️축약(Reduction)

* 스트림의 요소들을 하나의 값으로 축약하는 메소드이다.
* 매번 새 결과 객체를 만든다.

```
[1, 2, 3, 4, 5] 를 더하는 경우
1 + 2 = 3
    3 + 3 = 6
        6 + 4 = 10
            10 + 5 = 15  → 최종 결과

```

* `Optional<T> reduce(BinaryOperator)`
* `T reduce(identity, accumulator)`
    * 초기값(identity)을 직접 주는 형태
* `U reduce(identity, accumulator, combiner)`
    * 병렬용으로 combiner는 병렬로 나뉘어 계산된 부분 결과들을 합치는 역할을 한다.

### ⭐️ 수집(Collecting)

* 여러 개의 값을 하나의 객체로 모은다.
* 하나의 객체를 만들고 거기에 계속 추가한다.
* `collect(Collector)`
    * `Collectors.toList()` , `toSet()` , `groupingBy()` 등을 사용하여 값을 반환한다.
* `toList()`
    * 불변 리스트 반환
* `toArray()` / `toArray(IntFunction)`
    * 배열 반환

### 검사

* `boolean anyMatch(Predicate)`
    * 하나라도 만족(참)
* `boolean allMatch(Predicate)`
    * 모두 만족(참)
* `boolean noneMatch(Predicate)`
    * 모두 불만족(참)

### 검색

* `Optional<T> findFirst()`
    * 첫 번째 결과 요소 반환
* `Optional<T> findAny()`
    * 병렬에서 유리
---
### 쇼트서킷(short-circuit)이 가능한 메소드

* `anyMatch()`, `allMatch()`, `noneMatch()`, `findFirst()`, `limit()`
* 위 메소드들은 조건을 만족하면 전체를 순회하지 않고 즉시 종료