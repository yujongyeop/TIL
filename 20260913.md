# 1. 복제(Replication)

- 레플리카가 붙으면 마스터가 BGSAVE로 RDB를 만들어 전송하고, 그 사이에 들어온 쓰기 명령은 버퍼에 모아뒀다가 이어서 보낸다.

## 부분 재동기화

- 네트워크가 일시적으로 끊겼다고 매번 RDB를 통째로 다시 받으면 낭비이다.
- 마스터는 replication backlog라는 순환 버퍼에 최근 명령 스트림을 보관한다.
- 레플리카가 재접속하면 자기가 받은 마지막 offset을 알려주고, 그 지점이 버퍼 안에 남아 있으면 차이분만 받는다.(partial resync)

```conf
repl-backlog-size 64mb # 기본은 1mb인데 너무 작음
repl-backlog-ttl 3600
```

- 끊김이 잦거나 쓰기량이 많은 상황에서 backlog가 작으면 계속 full sync가 발생해 마스터의 fork와 네트워크를 잡아먹는다.
- `쓰기처리량(MB/s) * 예상 최대 단절 시간(초)` 정도로 잡는 게 기준이다.

## 읽기 분산 함정

- 레플리카로 읽기를 분산하는 건 흔한 패턴이지만, 비동기 복제 방식을 사용하여 방금 쓴 값이 안 보일 수 있다.(read-your-writes 위반)
- 쓰기 직후 반드시 읽어야 하는 값이라면 마스터로 보내야 한다.
- `[마스터] ← 끊김 → [레플리카A] [레플리카B] [Sentinel들]` 와 같이 마스터가 혼자만 단절 됐을 때 `Sentinel` 에 의해 레플리카A가 마스터로 승격되고, 기존 마스터가 다시 연결되면 마스터는 레플리카로 강등되어 다른 마스터(레플리카A)의 데이터를 받는다. 이 과정에서 **구 마스터가 끊긴 시간동안 쓰기 작업한 내용이 전부 사라진다.**
    - 아래의 설정을 하면 어느정도 위 현상을 발생하지 않게 보장해줄 수 있지만 완전한 보장은 아니다.
    - 이는 마스터가 네트워크 단절이 일어났을 때 본인이 레플리카가 없다는 걸 아는 데 시간이 걸리기 때문이다.

```conf
min-replicas-to-write 1 # 살아있는 레플리카가 1개 미만이면
min-replicas-max-lag 10 # 또는 lag 10초 초과면 쓰기 거부
```

---

# 2. Sentinel

- 마스터가 죽었을 때 레플리카 중 일부를 마스터로 승격 시키는 역할을 한다.

```conf
sentinel monitor mymaster 10.0.0.10 6379 2
sentinel down-after-milliseconds mymaster 5000
sentinel failover-timeout mymaster 60000
sentinel parallel-syncs mymaster 1
```

- Sentinel은 반드시 홀수로, 3개 이상, 그리고 서로 다른 물리 서버에 두어야 한다.
    - 2개면 하나만 죽어도 과반을 못 만들어 페일오버가 불가능하다.

> 💡 `quorum(정족수)`과 과반은 다르다.
>
> - **quorum**: 마스터가 죽었다고 판정하는 데 필요한 Sentinel 수
> - **과반**: 실제로 페일오버를 실행하는 데 필요한 Sentinel 수

## 페일오버 순서

1. 한 Sentinel이 마스터 응답 없음을 감지 → `SDOWN(주관적 다운)`
2. 다른 Sentinel들에게 물어봐 quorum 이상이 동의 → `ODOWN(객관적 다운)`
3. Sentinel들끼리 리더를 선출(`Raft` 알고리즘과 유사)
4. Sentinel 리더가 레플리카 중 하나를 고름 - 우선순위는 `replica-priority` → 복제 offset(최신) → runid 순
5. 선택된 레플리카에 `REPLICAOF NO ONE` , 나머지는 새 마스터를 바라보게 재설정
6. 구 마스터가 살아 돌아오면 자동으로 레플리카로 강등

> `replica-priority 0` 으로 두면 그 노드는 절대 마스터가 되지 않는다. 백업 전용 노드에 사용한다.

> 💡 클라이언트에서 마스터와 레플리카 주소를 가져오는 방식
>
> 클라이언트는 Redis가 아니라 Sentinel에 접속해 현재 마스터 주소를 물어봐야한다.
>
> 마스터 IP를 코드에 하드코딩하면 Sentinel을 아무리 잘 구성해도 소용없다.

---

# 3. Cluster

- 데이터를 쪼개서 노드에 분산하여 저장하는 역할을 한다.
- `cluster-require-full-coverage` 가 `yes`(기본값)면 슬롯 일부라도 담당 노드가 없을 때 **클러스터 전체가 쓰기를 거부한다**.
    - 캐시 용도라면 `no` 를 두어 살아있는 부분만이라도 서비스하는 게 낫다.

## 슬롯

- 키를 `CRC16(key) % 16384` 로 계산해 16384개 슬롯 중 하나에 배정하고, 슬롯 범위를 노드들이 나눠 갖는다.

```text
노드A: 0     ~ 5460
노드B: 5461  ~ 10922
노드C: 10923 ~ 16383
```

- 각 마스터는 자기 레플리카를 갖고, **마스터가 죽으면 Cluster 자체가 페일오버를 수행한다.**(Sentinel 불필요)
- 최소 구성은 마스터 3 + 레플리카 3 = 총 6노드 이다.
- 마스터가 3개 이상이어야 하는 이유는 페일오버 투표에 과반이 필요하기 때문이다.

## MOVED와 클라이언트

- 클라이언트가 엉뚱한 노드에 요청하면 `MOVED IP:PORT` 응답이 돌아온다.
- 클러스터 지원 클라이언트는 이를 받아 슬롯 맵을 갱신하고 요청을 재시도한다.
    - **즉 클라이언트가 클러스터를 알아야 한다.**
    - 일반 클라이언트가 붙으면 계속 리다이렉트만 받는다.
- 제약사항
    - 서로 다른 슬롯의 키를 한 명령에서 다룰 수 없다.
    - 같은 노드에 모으려면 **해시 태그**를 사용한다.(중괄호 안쪽만 해싱 대상이다.)

```text
    {user:1000}:profile
    {user:1000}:sessions    → 둘 다 동일 슬롯
```

- 태그를 남발하면 특정 슬롯에 데이터가 몰려 분산 효과가 사라진다.
    - 트랜잭션(MULTI), Lua 스크립트도 동일한 제약을 받는다.
- **Cluster 도입 결정은 키 설계를 먼저 끝내고 해야한다.**
    - 이미 운영중인 서비스를 나중에 클러스터로 옮기려다 멀티 키 연산 때문에 막히는 경우가 많다.

## 리샤딩

- 노드 추가는 슬롯을 옮기는 방식으로, 서비스 중단 없이 가능하다.

```bash
redis-cli --cluster add-node 10.0.0.4:6379 10.0.0.1:6379
redis-cli --cluster reshard 10.0.0.1:6379
redis-cli --cluster rebalance 10.0.0.1:6379
```

---

> 💡 세 기술은 동시에 사용하지 않고 용도에 따라 사용한다.
>
> - 데이터가 한 노드 메모리에 들어가고, HA만 필요한 상황이라면 복제 + Sentinel
> - 메모리나 쓰기 처리량이 단일 노드 한계를 넘을 경우 Cluster
> - 멀티 키 연산 및 트랜잭션을 많이 쓸 경우 Sentinel 유지, Cluster 신중히