# 테스트란?

- 입력에 대해 어떤 결과가 나와야 한다를 코드로 적어 판단하게 만드는 것이다.

# 테스트의 발전 과정

- 1단계: 앱을 띄워서 사람이 직접 하는 방식, 사람이 실행과 판정을 다 한다.
- 2단계: 메소드로 실행하고, 출력으로 사람이 확인하는 방식, 코드가 실행하고 사람이 판정한다.
- 3단계: 단언(assert)으로 판정까지 자동화, 코드가 실행과 판정을 한다.

> 실행과 판단의 주체가 사람에서 코드로 이동하는 방향으로 발전하였다.
>

# 테스트 구성 3 요소

- `준비(given)`: 검증할 상황을 만든다.
- `실행(when)`: 대상을 호출한다.
- `판정(then)`: 기대와 실제를 비교하고 다르면 실패시킨다.

# 테스트 원칙

- **판정이 없으면 테스트가 아니다**.
- **단언 없이 실행만 하는 코드는 커버리지만 올리는 가짜 테스트이다.**

# 테스트의 목적

1. 회귀 방지
    - 과거에 고친 버그가 나중에 되살아나는 것을 막는다.
    - 잘 돌아가는 코드를 보장하는 역할을 한다.
2. 설계 피드백
    - given절이 너무 길다 → 의존성이 너무 많다.
    - 서버를 띄워야 한다 → 로직이 프레임워크에 종속된다.
    - 뭘 검증할 지 모르겠다 → 책임이 불분명하다.
3. 실행 가능한 명세
    - 주석과 문서는 코드가 바뀌어도 수정되지 않을 수 있어 코드와 문서 사이에 차이가 발생할 수 있다.
    - 테스트는 기존 코드와 바꾼 코드 사이에서 차이가 발생하면 실패한다.
    - 즉, 거짓말을 할 수 없는 문서이다.
4. 피드백 단축
    - 코드로 실행하여 사람이 수동을 확인하는 거 대비 매우 빠르게 검증 가능하다.

# 테스트의 종류

## 1. 범위별

- 단위 테스트
    - 클래스/메소드 하나를 대상으로 하는 테스트이다.
    - 별로도 띄워야할 것이 없다.
- 통합 테스트
    - 여러 구성요소의 연결을 대상으로 테스트하며, DB, Redis, 스프링 컨텍스트 등을 띄워야 할 수 있다.
- E2E 테스트
    - 시스템 전체를 테스트하며, 실제 서버를 띄우고, 필요 시 브라우저까지 사용한다.
    - 속도가 제일 느리며, 실패의 원인을 찾기가 어렵다.

## 2. 검증 대상별

- 기능 테스트: 결과가 맞는 지 테스트한다.
- 비기능 테스트: 성능, 부하, 보안, 호환성 등을 테스트한다.

## 3. 목적과 시점별

- 회귀 테스트: 새 종류가 아니라 **기존 테스트를 다시 돌리는 행위이다.**
- 스모크 테스트: 배포 직후 “일단 켜지는 지 확인”만 보는 최소 집합의 테스트이다.
- 인수 테스트: 비즈니스 요구가 충족되는 지 확인한다.(보통 E2E 범위 내이다.)

# POJO

<aside>
💡

POJO(Plain Old Java Object)

특정 프레임워크를 상속·구현하지 않은 평범한 자바 객체를 의미한다.

</aside>

## POJO의 이점

- **테스트가 가능해진다**: `new`로 만들 수 있으면 테스트는 자동으로 쉬워진다
- **비즈니스 로직이 기술에 오염되지 않는다**: "재고 부족 시 주문 실패"는 비즈니스 규칙이지 Spring의 관심사가 아니다.
- **프레임워크에 종속되지 않는다.**: MVC → WebFlux, JPA → JDBC를 바꿔도 핵심 로직은 그대로이다.
- **읽기 쉽다**: 프레임워크 지식 없이 의도를 알 수 있다

> 테스트 코드와 비즈니스 로직은 POJO로 작성하는 것을 원칙으로 한다.
>