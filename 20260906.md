# Tool Calling(툴 호출)이란?

- **AI 모델이 애플리케이션이 정의한 함수를 호출하고 그 결과에 따라 행동하는 능력**이다.
- **툴 호출을 모델의 능력이라 부르지만, 툴 호출 로직을 소유하는 건 애플리케이션이다.**
    - 모델은 툴 호출을 요청하고 입력 인자를 제공할 수 있을 뿐, 실행하고 결과를 돌려주는 건 애플리케이션 책임이다.
    - 또한, 모델은 툴 뒤에 있는 API에 직접 접근하지 못한다.
        - 아래 코드 기준으로 "사용자 기준으로 현재 날짜와 시간을 알려줘"라고 모델이 요청하면, `LocalDateTime` 객체를 사용하는 내부 코드는 모델이 접근할 수 없고, 오직 반환에만 접근할 수 있다는 의미이다.

```java
class DateTimeTools {

    @Tool(description = "Get the current date and time in the user's timezone")
    String getCurrentDateTime() {
        return LocalDateTime.now().atZone(LocaleContextHolder.getTimeZone().toZoneId()).toString();
    }

    @Tool(description = "Set a user alarm for the given time, provided in ISO-8601 format")
    void setAlarm(String time) {
        LocalDateTime alarmTime = LocalDateTime.parse(time, DateTimeFormatter.ISO_DATE_TIME);
        System.out.println("Alarm set for " + alarmTime);
    }
}
```

## 용도

> **💡 `ChatBot`과 `Agent`**
>
> Spring AI 공식 문서에서 텍스트만 생성할 수 있는 모델은 `ChatBot`, 정보를 찾고 행동을 취하고 목표에 도달할 때까지 루프를 돌 수 있는 모델은 `Agent`라고 정의되어 있다.

- `정보 검색`
    - DB, 웹, 파일 시스템, 검색 엔진의 데이터로 모델 지식을 보강하는 것이다.
    - 날씨 조회, 고객 레코드 질의 같은 것이 이에 해당한다.
- `행동 수행`
    - 이메일 전송, 항공권 예약, 레코드 갱신, 워크플로우 트리거 등이 해당한다.

# 툴 정의 방식

## 1. 선언적 `@Tool`

- 메소드를 직접 소유하고 있고 최소한의 절차를 원할 때 사용한다.
- 접근 제어자, static 여부, 인스턴스 메소드 여부와 관계없이 사용할 수 있다.
- `@ToolParam`의 기본값은 필수이다.
    - 필수 여부를 올바르게 정의하는 것이 환각을 줄이는 데 결정적인 도움이 된다.

### 지원하는 속성

- `name`
    - 툴의 이름이며, 기본값은 메소드 이름이다.
    - 모델에 제공되는 툴 집합 안에서 유일해야 한다.
- `description`
    - 무엇을 하고 언제 써야 하는지를 나타내며, 사용하는 것을 강력히 권장한다.
    - 없을 경우 모델에게 호출 시점에 대한 지침이 전혀 없다.
- `returnDirect`
    - 결과를 모델에 다시 넣지 않고 호출자에게 바로 반환한다.
- `resultConverter`
    - 사용할 `ToolCallResultConverter`를 지정한다.

```java
@Tool(description = "Get the weather for a city at a specific time")
public String getWeather(
        @ToolParam(description = "City name") String city,
        @ToolParam(description = "Time in ISO-8601 format", required = false) String at) {
    return weatherService.fetch(city, at);
}
```

## 2. `MethodToolCallback`

- 메소드 기반 툴에 프로그래밍 방식 제어가 필요할 때 사용한다.
    - **소스 클래스를 통제하지 못할 때**
        - 서드파티 라이브러리의 메소드를 툴로 노출하고 싶지만 해당 클래스에 어노테이션을 붙일 수 없다.
    - **툴 정의를 런타임에 만들고 싶을 때**
        - 값이 DB나 설정 파일에서 오는 경우
        - 테넌트나 사용자 권한에 따라 노출할 툴이 달라지는 경우

### 지원하는 메소드

- `toolDefinition`
    - 모델이 보는 정보로 이름, 설명, 입력 스키마 정보가 들어간다.
    - `ToolDefinitions.builder(method)`가 메소드 시그니처에서 이름과 스키마를 자동 생성해줘서 보통 설명만 추가한다.
- `toolMethod`
    - 실제 호출할 메소드를 지정한다.
    - 리플렉션을 통해 메소드를 찾아온다.
- `toolObject`
    - 메소드를 호출할 대상 인스턴스를 지정한다.
    - **인스턴스 메소드면 필수이고, static 메소드면 선택이다.**

### `@Tool`과의 관계

- 별개의 메커니즘이 아니다.
- `@Tool`을 붙였을 때 Spring AI가 내부적으로 만들어주는 것은 결국 `MethodToolCallback`이다.
    - 어노테이션은 그 과정을 감춰주는 편의 계층이다.

```java
Method method = ReflectionUtils.findMethod(WeatherTools.class, "getWeather", String.class);

MethodToolCallback callback = MethodToolCallback.builder()
    .toolDefinition(ToolDefinitions.builder(method)
        .description("Get the current weather for a given city")
        .build())
    .toolMethod(method)
    .toolObject(new WeatherTools())
    .build();
```

## 3. `FunctionToolCallback`

- `Function` / `Supplier` / `Consumer` 람다나 메소드 참조를 노출할 때 사용한다.
- 실무에서 많이 사용하려는 부분이다.
    - 이미 서비스 빈에 메소드가 있고 리플렉션까지 갈 필요 없이 메소드 참조 하나로 끝내고 싶을 때
    - 툴 로직이 짧아서 별도 클래스를 만들기 아까울 때
    - 툴을 `@Bean`으로 등록해 재사용하고 싶을 때
- 다른 방식과의 결정적인 차이는 **입력 타입을 명시해야 한다**는 점이다.
    - `@Tool`과 `MethodToolCallback`은 메소드 시그니처가 남아 있어서 파라미터 이름, 타입, 개수를 리플렉션으로 읽을 수 있고, 그렇기 때문에 스키마를 자동으로 생성할 수 있다.
    - 반면, 람다는 **타입 소거 때문에 런타임에 제네릭 인자 타입을 알 수 없다.**
        - Ex. `Function<WeatherRequest, String>`은 런타임에서 그냥 `Function`이다.

### 지원하는 메소드

- `description`
    - `@Tool`의 `description`과 동일
- `inputType`
    - JSON 스키마 생성에 사용된다.

```java
@Configuration(proxyBeanMethods = false)
class WeatherToolsConfig {

    @Bean
    ToolCallback currentWeather(WeatherService weatherService) {
        return FunctionToolCallback.builder("currentWeather", weatherService::getWeather)
            .description("Get the weather in location")
            .inputType(WeatherRequest.class)
            .build();
    }
}
```

```java
// 다른 곳에서 사용할 때
@Autowired ToolCallback currentWeather;
```

---

# 정리

| 방식 | 정의 시점 | 대상 | 스키마 생성 |
| --- | --- | --- | --- |
| `@Tool` | **컴파일 타임** | 내가 소유한 메소드 | 자동 |
| `MethodToolCallback` | 런타임 | 아무 메소드나(리플렉션) | 자동 |
| `FunctionToolCallback` | 런타임 | 람다, 메소드 참조, 함수형 인터페이스 | `inputType` 명시 |