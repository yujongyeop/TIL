# LLM의 무상태

- LLM은 무상태이다.
    - 즉, 이전 상호작용에 대한 정보를 유지하지 않는다.
- 그렇기 때문에 이전 대화를 매번 프롬프트에 실어 보내는 수밖에 없다.
- 이를 대신해주는 객체가 `ChatMemory`이다.

# `ChatMemory`와 `ChatHistory`

- `ChatMemory` - LLM이 대화 중 맥락을 유지하는 데 실제로 쓰는 정보이다.
    - `ChatMemory`는 휘발성이고, 목적이 다르기 때문에 감사 로그를 남기려고 `ChatMemory`를 사용해서는 안 된다.
- `ChatHistory` - 사용자와 모델이 주고받은 전체 대화 기록이다.

# `ChatMemory`와 `ChatMemoryRepository`

- `ChatMemory`는 어떤 메시지를 남길지 결정하는 **전략**에 가깝다.
    - 어떤 메시지를 유지하고 언제 제거할지는 구현이 결정한다.
    - Spring AI가 `ChatMemory` 빈을 자동 설정해준다.
- `ChatMemoryRepository`는 `ChatMemory`를 저장하고 꺼내오기만 하는 **저장소**이다.
    - 기본 설정은 `InMemoryChatMemoryRepository`(`ConcurrentHashMap`) + `MessageWindowChatMemory`이다.

# `conversationId`

- 모든 chat memory 연산은 `conversationId`를 키로 한다.
    - 대화를 구분할 수 있는 유일한 키값이기 때문에 Spring AI 2.0부터 필수가 되었다.
- 이 파라미터는 모든 메모리 어드바이저에서 필수이며, 빠뜨릴 경우 런타임에서 `IllegalArgumentException`이 발생한다.

> **💡 conversationId 기본값**
>
> Spring AI 1.x에서는 `ChatMemory.DEFAULT_CONVERSATION_ID` 상수가 존재했다. 하지만 이는 Spring AI 2.0으로 넘어오면서 삭제되어 사용 시 컴파일이 되지 않는다.

- Spring AI에서 `conversationId` 관련 지침
    1. 사용자마다(한 사용자가 여러 대화를 가지면 대화마다) 별개 ID를 부여할 것
    2. 고정값을 재사용하지 말고 서버에서 파생시킬 것
    3. 대화를 나열하거나 삭제할 때 전체가 아니라 사용자에게 속한 ID만 대상으로 할 것

# `MessageWindowChatMemory`

- 내장된 유일한 메모리 타입이자 기본값이다.
- 지정한 최대 크기까지 메시지의 슬라이딩 윈도우를 유지하고, 초과하면 오래된 것부터 밀어내되 **`SystemMessage`는 항상 보존**한다.
- 기본 윈도우 크기는 20개이다.
- Spring AI 2.0에서는 축출이 필요할 때 **중간을 자르지 않고 항상 턴 단위로 통째로 제거**한다.
    - 잘라낼 지점이 유저 메시지가 아닌 곳(Ex. 툴 호출 교환 중간의 어시스턴트 응답)에 걸리면, 다음 `UserMessage`까지 앞으로 밀어서 남는 윈도우가 항상 완전한 턴에서 시작할 수 있도록 한다.
    - 따라서, **`maxMessages`는 상한선일 뿐 실제 개수는 그보다 적을 수 있다.**

> **💡 턴이란?**
>
> `UserMessage`에서 시작해 다음 `UserMessage` 직전까지의 모든 어시스턴트 응답, 툴 호출, 툴 응답을 포함하는 단위이다.

# 저장소 종류

| 구현체 | 의존성 | 비고 |
| --- | --- | --- |
| `InMemoryChatMemoryRepository` | 기본 제공 | `ConcurrentHashMap`, 재시작 시 날아감 |
| `JdbcChatMemoryRepository` | `spring-ai-starter-model-chat-memory-repository-jdbc` | PostgreSQL, MySQL, SQL Server, HSQLDB, Oracle |
| `CassandraChatMemoryRepository` | `spring-ai-starter-model-chat-memory-repository-cassandra` | 시계열 스키마, TTL 지원 |
| `Neo4jChatMemoryRepository` | `spring-ai-starter-model-chat-memory-repository-neo4j` | 그래프 |
| `MongoChatMemoryRepository` | `spring-ai-starter-model-chat-memory-repository-mongodb` | TTL 지원 |
| `RedisChatMemoryRepository` | `spring-ai-starter-model-chat-memory-repository-redis` | Redis Stack 7.0+, 고급 쿼리 |

- `spring.ai.chat.memory.repository.jdbc.initialize-schema`는 `embedded`(기본), `always`, `never` 세 가지 설정값이 있다.
    - `embedded` - 임베디드 DB에서만 초기화
    - `always` - 항상 초기화
    - `never` - 초기화하지 않음