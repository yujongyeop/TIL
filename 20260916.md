# 상태 기계 (State Machine)

- 어떤 시스템이 가질 수 있는 상태(State)를 미리 정해두고, 어떤 사건(Event)이 일어났을 때 어느 상태로 옮겨갈지를 규칙으로 못 박아둔 모델이다.
- `FSM` — 시스템이 가질 수 있는 유한한 개수의 상태 중 단 하나의 상태만 유지하며, 특정 이벤트나 조건에 따라 다른 상태로 변화(전이)하는 수학적 모델이자 프로그래밍 디자인 패턴이다.
- 시작 상태가 있으며 종료 상태는 선택 사항이다.

## 주요 구성 요소

### 1. 상태(State)

- 시스템이 취할 수 있는 특정 조건이나 모드이다.
- Ex. 신호등의 빨간불, 초록불, 노란불

### 2. 이벤트(Event)

- 상태를 바꾸게 만드는 외부 자극이나 조건이다.
- Ex. 타이머 만료 또는 버튼 입력

### 3. 전이(Transition)

- 한 상태에서 다른 상태로 이동하는 과정이다.
- Ex. 빨간불에서 초록불로 바뀌는 이동 과정

### 4. 행동(Action)

- 상태가 변하거나 유지될 때 시스템이 실행하는 작업이다.
- Ex. 초록불 상태일 때 보행자 신호 소리 출력

## 장단점

### 장점

- **불가능한 상태를 구조적으로 차단 가능하다.**
    - 예를 들어 배송 완료된 주문을 "다시 결제 대기로 되돌리기" 같은 전이가 아예 정의되어 있지 않으면, 실수로 그런 코드를 짜도 런타임에서 터진다.
    - `isPaid=false`와 `isShipped=true` 같은 말이 안 되는 조합이 표현 가능해지는데, 상태 기계는 이런 조합을 만들 수 없게 만든다.
- **복잡한 조건문이 사라진다.**
    - `if (isPaid && !isCancelled && !isShipped && hasStock)` 같은 중첩 조건문이 프로젝트 전역에 퍼져 있는데, 이를 규칙을 정의하는 한 곳에 모은다.
- **동작을 예측하고 검증하기 쉽다.**
    - 현재 상태와 입력만 알면 다음 상태가 결정된다.
- **문서와 코드가 일치한다.**
    - 상태 전이도를 그리면 그게 곧 명세이자 구현 설계이다.

### 단점

- **상태 폭발(state explosion)**
    - 서로 독립적인 조건이 늘어날수록 상태가 곱셈으로 증가한다.
    - Ex. 로그인 여부 × 연결 여부 × 동작 모드 = 8일 때, 조건 하나가 추가되면 16개가 된다.
- **연속적이거나 데이터 중심인 문제에 안 맞는다.**
    - 상태가 명확히 나뉘지 않는 도메인에 적합하지 않다.
    - Ex. 실수 값을 계속 추적하거나 복잡한 데이터를 변환하는 작업
- **계산 능력이 제한적이다.**
    - FSM은 상태 하나 외에는 메모리가 없다.
    - 괄호 짝 맞추기나 "지금까지 몇 개 봤는지 세기" 같은 건 원리적으로 불가능하다.
    - 실무에서는 상태 외에 별도 변수를 두어 우회하는데, 이 변수가 늘어나면 상태 기계의 장점인 검증 가능성이 그만큼 깎인다.
- **전이 규칙 수정이 번거롭다.**
    - 요구사항이 바뀌어 전이 하나를 추가하려면 전이표, 다이어그램, 테스트를 모두 손봐야 한다.