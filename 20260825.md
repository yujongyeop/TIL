## 중간 연산

### 1. 스트림 자르기

* `skip(long n)` - 앞 n개의 요소 건너 뛴다.
* `limit(long maxSize)` - 스트림의 요소를 maxSize개로 제한한다.

### 2. 스트림의 요소 걸러내기

* `filter(Predicate<? super T> predicate)`
    * 주어진 조건(Predicate)에 맞지 않는 요소를 걸러낸다.
* `distinct()`
    * 스트림의 중복되는 요소를 제거한다.

### 3. 정렬

* `sorted()` or `sorted(Comparator comparator)`
    * Comparator로 스트림을 정렬한다.
    * Comparator를 지정하지 않으면 스트림 요소의 기본 정렬 기준으로 정렬한다.(단, 스트림의 요소가 Comparable을 구현한 클래스가 아니면 예외가 발생한다.)

### 4. 변환

* `map()`
    * 스트림의 요소에 저장된 값 중에서 원하는 필드만 뽑아내거나 특정 형태로 변환한다.

### 5. 조회

* `peek()`
    * 연산과 연산 사이에 올바르게 처리되었는지 확인하는 메소드이다.
    * `forEach()`와 달리 스트림의 요소를 소모하지 않는다.

### 6. `flatMap()`

* 스트림의 요소가 배열이거나 map()의 연산결과가 배열인 경우(`Stream<T[]>`), 이를 `Stream<T>` 로 변환해준다.

`map()` 과의 차이

```java
Stream<String[]> strArrStrm = Stream.of(...);

//map 사용 시
Stream<Stream<String>> strStrStrm = strArrStrm.map(Arrays::stream);
//flatMap 사용 시
Stream<String> strStrm = strArrStrm.flatMap(Arrays::stream);
```

## `@Value` 필드 값 주입

* 필드 주입(Field Injection)과 동일한 시점에 처리된다.
* `AutowiredAnnotationBeanPostProcessor` 에 의해 `@Value` 가 붙은 필드에 값이 주입된다.
    * 해당 Processor는 필드 주입(`@Autowired`)에 대한 처리도 담당한다.

> 💡 **생성자 주입은 누가 처리하나요??**
>
> * 생성자 주입은 `AbstractAutowireCapableBeanFactory` 에서 처리하며, 이 Factory는 인스턴스 생성 단계 이전에 이미 생성된 상태이다. 더 정확하게는 `AbstractAutowireCapableBeanFactory` 는 **빈 컨테이너 그 자체**이다.

### 값이 주입되는 시점

> 💡 생성자 호출 후, `@PostConstruct` 실행 전에 주입된다.

1. 인스턴스 생성(생성자 호출)
2. `field/setter` 주입 ← 이 시점에 `@Value`가 처리된다.
3. `@PostConstruct` , `InitializingBean.afterPropertiesSet()` , `@Bean(initMethod)` 등의 초기화 콜백
4. 빈 사용 가능