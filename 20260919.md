# `sealed` 클래스

- 상속 계층을 명시적으로 제한하는 기능이다.
- 상속을 통제하는 방법은 두 가지뿐이다.
    - `final`로 완전히 막는다.
    - `sealed`로 막는다.

```java
public sealed class Shape permits Circle, Rectangle, Triangle { }

public final class Circle extends Shape {
    private final double radius;
    // ...
}

public final class Rectangle extends Shape { }
public non-sealed class Triangle extends Shape { }
```

- `sealed`는 `final`과 다르게 상속을 완전히 막는 것이 아니라 **지정한 클래스를 제외한 다른 클래스의 상속을 막는다.**
- 그렇기 때문에 **`sealed`는 `final`과 함께 사용하면 안 된다.**

## 규칙

1. **허용된 하위 클래스는 반드시 셋 중 하나를 선언해야 한다.**

   | 키워드 | 의미 |
      | --- | --- |
   | `final` | 더 이상 상속 불가 |
   | `sealed` | 자기만의 `permits` 목록을 가짐 |
   | `non-sealed` | 누구나 상속 가능 |

2. **같은 모듈(또는 이름 없는 모듈이면 같은 패키지)에 있어야 한다.**
    - 외부 라이브러리에서 끼어들 수 없게 하려는 의도이다.
3. **같은 파일 안에 있으면 `permits` 생략 가능하다.**

```java
public sealed interface Payment {
    record Card(String number) implements Payment { }
    record Cash(int amount) implements Payment { }
}
```

## `sealed` 인터페이스

- 가능한 경우의 수가 정해져 있을 때 사용한다.
    - Ex. 결제 수단의 종류: `Card`, `Cash`, `Points`
    - Ex. HTTP 응답 실패·성공
- 하위 인터페이스에는 `final`이 없다.
- 장점: `default`가 없는데도 컴파일이 가능하다.

```java
String describe(Payment p) {
    return switch (p) {
        case Card c    -> "카드 " + c.last4();
        case Cash c    -> "현금 " + c.amount() + "원";
        case Points p2 -> "포인트 " + p2.amount() + "점";
        // switch문에 default가 없어도 정상 동작한다.
    };
}
```

## `permits`에 올 수 있는 것

| 종류 | 가능 여부 | 비고 |
| --- | --- | --- |
| 하위 인터페이스 | 가능 | `extends`로 연결 |
| 일반 클래스 | 가능 | `final`/`sealed`/`non-sealed` 명시 필요 |
| record | 가능 | 암묵적 `final` |
| enum | 가능 | 암묵적 `final` |
| 제네릭 타입 변수 | 불가능 | Ex. `sealed interface Container<T> permits T` |
| 익명 클래스·지역 클래스 | 불가능 | - |
| 람다 | 불가능 | - |

# `non-sealed` 클래스

- 누구나 상속 가능한 클래스를 의미한다.
- 자바는 `sealed`의 하위 클래스에는 무조건 `non-sealed`, `sealed`, `final` 중 하나를 붙여야 한다.
    - 미사용 시: `non-sealed, sealed, or final modifiers expected` 예외 발생