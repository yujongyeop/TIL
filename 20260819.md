# 최상위 부모 클래스(Object)

`java.lang.Object`는 자바 클래스 계층의 **최상위 클래스**다.

> 💡 `java.lang` 패키지에 있는 클래스들은 import문 없이 바로 사용 가능하다.

## 특징

- 유일하게 부모가 없는 클래스
- 클래스 내에 필드가 존재하지 않음
- `native` 메소드를 사용
> 💡 **native 메소드란?**
>
> Java 코드가 아닌 다른 언어(주로 C/C++)로 구현된 메소드를 말한다.
>
> Java 쪽에는 시그니처(일종의 메소드명)만 있고 본문이 없으며, 실제 구현은 JNI(Java Native Interface)를 통해 연결된다.
>
> 주로 JVM 내에서 할 수 없는 기능(OS/하드웨어 레벨 접근)과 성능 최적화(암호화/영상 처리 등)에 사용된다. 혹은 자바 코드로는 접근할 수 없는 JVM 내부 정보를 다루기 위해 사용된다.

## 제공 메소드

### 1. toString()

- `객체의 클래스명 + @ + 16진수 해시코드`를 반환
    - 메모리 주소를 반환하지 않음
- 출력이나 로깅에 자주 사용되기 때문에 **사실상 Overriding이 필수**
- 콘솔이나 로그에 객체만 입력하면 자동으로 `toString()`을 호출
```java
Car car = new Car();
System.out.println(car);  // → System.out.println(car.toString());
```

### 2. equals(Object) — 동등성 비교

- 참조(reference) 비교(`==`)를 지원
- 논리적 동등성을 원할 경우 Overriding 필요
#### 5가지 원칙 준수

| 원칙 | 내용 |
| --- | --- |
| 반사성 | `a.equals(a)`는 항상 `true` |
| 대칭성 | `a.equals(b)`가 `true`면 `b.equals(a)`도 `true` |
| 추이성 | `a=b`이고 `b=c`이면 `a=c` |
| 일관성 | 객체가 변하지 않는 한 몇 번을 호출해도 같은 결과 반환 |
| null 비교 | `a.equals(null)`은 항상 `false` |

### 3. hashCode()

- 객체를 32비트 정수로 요약하는 메소드
- **재정의하지 않으면** 필드 값이 같아도 해시코드는 **다르다** (identity hash code)
    - 필드 값이 같을 때 같은 해시코드를 원한다면 반드시 Overriding 필요
- `equals()`를 재정의하면 `hashCode()`도 **반드시 함께** 재정의해야 함
    - `equals`가 `true`인 두 객체는 같은 해시코드를 반환해야 한다는 규약
    - 어기면 `HashMap`, `HashSet`에서 넣은 객체를 다시 찾지 못함
- 반대는 성립하지 않음 — 해시코드가 같아도 다른 객체일 수 있음(해시 충돌)
> 📌 Collection 프레임워크에서 더 자세히 다룰 예정 (2026-08-20)

### 4. getClass()

```java
public final native Class<?> getClass();
```

- 런타임 타입의 `Class` 객체를 반환
- `final` 메소드라 Override 할 수 없음
    - 재정의를 통해 타입 정보를 위조하면 Reflection의 모든 게 무너지기 때문에 막음
### 5. clone()

```java
protected native Object clone() throws CloneNotSupportedException;
```

- 기본적으로 **얕은 복사(shallow copy)** 를 지원
    - 참조 타입 필드는 원본과 같은 객체를 가리킴
    - 실무에서는 별도의 복사 생성자나 정적 팩토리 메소드를 쓰는 것을 권장
- 접근제어자가 `protected`이기 때문에 외부에서 호출하려면 `public`으로 Overriding 필요
- `Cloneable` 인터페이스를 구현하지 않으면 `CloneNotSupportedException`이 발생
### 6. 그 밖의 메소드들

#### `wait()`, `notify()`, `notifyAll()`

- 모든 객체가 모니터(monitor)를 가진다는 설계에서 나온 메소드
- 모두 `final` 메소드
- `synchronized` 블록 안에서만 호출할 수 있고, 그 외에서 호출할 경우 `IllegalMonitorStateException`이 발생
> 💡 현대 자바에서는 저수준 API보다 `java.util.concurrent`의 `ReentrantLock`, `Condition`, `CountDownLatch` 등을 쓰는 것이 안전하고 표현력이 좋다고 함 → 나중에 알아봐야지…
 
---

# Q. 모든 클래스가 Object를 어떻게 상속받는가?

## A. 묵시적 상속으로 상속받는다.

Java Compiler는 클래스가 `extends`로 다른 클래스를 상속하지 않았을 때 암묵적으로 `extends Object`를 붙여준다.

```java
// 개발자 작성 코드
class A {}
 
// 컴파일러가 수정한 코드
class A extends Object {}
```
 
