# 하네스 엔지니어링

> 💡 **하네스란?**
>
> 말(Horse)을 제어하기 위해 채우는 장비를 의미한다.
>
> 에이전트 = Model + 하네스

- AI 도구를 사용할 때 처음에는 `"~~해 줘"` 같은 **프롬프트** 방식을 사용했다.
- 이후에는 컨텍스트가 나오고, 현재는 AI가 직접 파일을 읽고, 코드를 쓰고, 테스트하고, PR까지 하는 에이전트 방식까지 발전했다.

  > 프롬프트와 컨텍스트는 **입력**을 개선하는 방식이고, 하네스는 **환경**을 설계하는 방식이다.

| 구분 | 프롬프트 | 컨텍스트 | 하네스 |
| --- | --- | --- | --- |
| 범위 | 1회 입력 | 1세션 | 영구 시스템 |
| 지속성 | 없음 | 세션 내 | 프로젝트 전체 |
| 주체 | 개발자가 지시 | 개발자가 안내 | 환경이 제약 |
| 실수 대응 | 재입력 | 재설명 | 구조적 방지 |
| 핵심 기술 | 프롬프트 작성 | 컨텍스트 구성 | 하네스 설계 |

## AI 사용 시 문제점

- 컨벤션을 무시하거나 없던 파일을 만들고, 이미 결정한 사항을 뒤집는 등의 행동을 할 수 있었다.
    - 이는 사용자(개발자)가 에이전트에게 규칙을 주지 않고 실행하여 생기는 문제 중 하나였다.
- AI가 작성한 코드가 기존 코드 스타일과 불일치할 수 있다.
- 팀이 정해둔 코드 구조를 무시할 수 있다.
- 한 번 수정한 문제가 다음 세션에서 다시 나타나는 같은 실수를 반복할 수 있다. 등등

**→ 즉, 하네스 엔지니어링은 AI 도구를 사용할 때 너무 많은 자율성을 줌으로 인해 생기는 문제를 해결하고자 나온 엔지니어링 방식이다.**

> ⚠️ 하네스 엔지니어링은 에이전트를 똑똑하게 만드는 기술이 아니다.
>
> 에이전트가 **올바르게 행동할 수밖에 없는 환경**을 설계하는 기술이다.

## 하네스의 구성

### 1. 지시 문서

- 에이전트에게 규칙을 전달한다.
- `AGENTS.md`, `CLAUDE.md` 같은 파일 형식이다.
- 코딩 스타일, 네이밍 규칙, 접근하면 안 되는 파일과 디렉토리, PR 및 커밋 작성 방식 등의 내용을 넣는다.

### 2. 아키텍처 제약

- 구조적으로 잘못된 코드를 차단한다.
- `Linter`, 디렉토리 규칙처럼 코드가 저장되거나 병합되기 전에 자동으로 검사하는 장치이다.
- 허용되지 않는 import 경로 차단, 코드 스타일 자동 교정, 특정 패턴 사용 금지 등의 내용을 넣는다.

### 3. 피드백 루프

- 에이전트 행동을 실시간으로 교정한다.
- 에이전트가 작업한 결과가 올바른지 즉시 알려주는 장치이다.
- 비르기타 뵈켈러는 두 가지로 분류한다.

| 종류 | 역할 | 예시 |
| --- | --- | --- |
| 가이드(Guide) | 에이전트가 행동하기 전에 미리 방향을 잡아주는 것 | 스타일 가이드, 템플릿, 코딩 컨벤션, 좋은 출력 예시 등 |
| 센서(Sensor) | 행동한 후에 관찰해서 스스로 교정하게 하는 것 | 린터, 테스트, 구조 검사 등 |

### 4. 지식 저장소(ADR)

- 사람의 결정과 맥락을 축적한다.
- `docs/` 디렉토리에 사람이 내린 결정과 채택한 이유, 포기한 대안 등을 저장한다.
- 왜 이 라이브러리를 선택했는가, 이전에 시도했다가 실패한 방법, 특정 구조를 유지하는 이유 등

```text
프로젝트 루트/
│
├── AGENTS.md          ← ① 지시 문서
├── .eslintrc          ← ② 아키텍처 제약
│
├── tests/             ← ③ 피드백 루프
│   └── ...
│
└── docs/              ← ④ 지식 저장소
    ├── decisions/
    └── conventions/
```
### 하네스가 아닌 것

| 개념 | 하네스와의 차이 |
| --- | --- |
| 프롬프트 엔지니어링 | 입력을 개선하는 기술, 하네스는 환경을 설계하는 기술 |
| 코딩 컨벤션 문서 | 사람을 위한 문서, 하네스는 에이전트가 읽고 따르는 구조 |
| CI/CD 파이프라인 | 하네스의 일부가 될 수 있지만, 하네스 전체가 아니다. |
---
# Q. 그럼 어디까지 하네스로 강제해야 하는가?

- 하네스로 너무 많은 제약을 하면 AI의 성능이 떨어질 수 있다.
- 그렇기에 일정 부분을 제약하지 않으며 성능을 높일 필요가 있다.
- 하네스로 강제하지 않아도 되는 부분
  - 판정이 결정론적인 내용
    - 결정론 - import 방향이 A → B인가?
    - 비결정론 - 이 추상화가 적절한가?
  - 되돌리기 쉬운가?
    - 되돌리기 쉬운 건 강제할 이유가 없다.
    - 공개 API, 스키마 변경, 외부 시스템과의 계약 등은 고비용 작업이다.