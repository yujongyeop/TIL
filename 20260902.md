# ChatClient란?

> 어드바이저 체인을 실행해 LLM 호출 한 건을 완성하는, 무상태 플루언트 클라이언트이다.
>
- 플루언트 클라이언트 - RestClient와 같은 결이며, 조립하고, 보내고, 원하는 타입으로 받는다.
- 어드바이저 체인 - 메모리, RAG, 툴 루프, 출력 검증이 전부 여기서 일어난다.
- 무상태 - 싱글턴으로 두고 여러 스레드가 같이 써도 된다.
- 즉, **Spring AI의 RestClient(HTTP 클라이언트)**와 유사하다.

# `ChatClient.Builder` 메소드

## 1구간 - ChatClient.Builder

| 메소드 | 용도 |
| --- | --- |
| `defaultSystem<String 등>` | 기본 시스템 텍스트 |
| `defaultUser<String 등>` | 기본 유저 텍스트 |
| `defaultOptions (ChatOptions)` | 포터블 옵션 또는 각 모델별 옵션 |
| `defaultTools(Object...)` | 모든 요청에 쓸 툴 등록 |
| `defaultToolContext(Map<String, Object>)` | 툴 실행 기본 컨텍스트 |
| `defaultAdvisors(Advisor... / List / Consumer<AdvisorSpec>)` | 기본 어드바이저 |
| `defaultTemplateRenderer(TemplateRenderer)` | 기본 템플릿 렌더러 |

## 2구간 - prompt() 진입

- 3가지 오버로드가 있다.
    - `prompt()` - 플루언트 체인 시작
    - `prompt(Prompt)` - 비플루언트 API로 만든 `Prompt` 전송
    - `prompt(String)` - 유저 텍스트만 간단히 넘긴다.

## 3구간 - ChatClientRequestSpec(요청별 설정)

- 1구간과 짝을 이룬다.
- `default` 접두사만 뗀 같은 이름이고, 여기서 부르면 기본값을 덮어쓴다.

| 메서드 | 비고 |
| --- | --- |
| `system(String / Resource / Consumer<PromptSystemSpec>)` |  |
| `user(String / Resource / Consumer<UserSpec>)` |  |
| `messages(Message... / List<Message>)` | 메시지 직접 주입 |
| `options(ChatOptions.Builder)` | **빌더를 커스터마이저로 받음** — 부분 지정 가능 |
| `tools(Object...)` |  |
| `toolContext(Map<String, Object>)` |  |
| `advisors(Advisor... / List / Consumer<AdvisorSpec>)` |  |
| `templateRenderer(TemplateRenderer)` |  |
| `mutate()` | 현재 요청 설정이 복제된 `Builder` 반환 |

### 3구간 서브 스펙

```java
.user(u -> u.text("{composer}가 작곡한 영화 5편")
						.param("composer", "조용필")
						.metadata("userId", "user-456"))
```

- `user()`와 `system()`에 `Consumer`를 넘기면 그 안에서 또 체이닝한다.
- `text(…)`/`param(k, v)`/`params(Map)` - 템플릿 변수
- `metadata(k, v)` /`metadata(Map)` - 메시지 메타데이터이다.(키와 값 모두 null이면 `IllegalArgumentException` 발생)

## 4구간 - `call()` 이후

| 메서드 | 반환 | 비고 |
| --- | --- | --- |
| `content()` | `String` | 응답 문자열 |
| `chatResponse()` | `ChatResponse` | 메타데이터, 토큰 사용량 포함 |
| `chatClientResponse()` | `ChatClientResponse` | `ChatResponse` + 어드바이저 실행 컨텍스트(RAG 검색 문서 등) |
| `entity(Class<T>)` | `T` | 특정 엔티티 타입으로 매핑 |
| `entity(ParameterizedTypeReference<T>)` | `T` | `List<T>` 등 제네릭 컬렉션 |
| `entity(StructuredOutputConverter<T>)` | `T` | 컨버터 직접 지정 |
| `responseEntity(Class<T>)` | `ResponseEntity` | `ChatResponse`와 엔티티를 함께 |
| `responseEntity(ParameterizedTypeReference<T>)` | `ResponseEntity` | 위와 동일, 컬렉션 |
| `responseEntity(StructuredOutputConverter<T>)` | `ResponseEntity` | 위와 동일, 컨버터 지정 |

# 인증 모드

## 1. API 키 방식

- `spring.ai.google.genai.api-key=키`
- Google AI Studio에서 발급한 키를 사용한다.

## 2. Vertex AI 방식

- Google Cloud의 자격증명을 사용한다.
- `spring.ai.google.genai.project-id=프로젝트_ID`
- `spring.ai.google.genai.location=로케일 정보`

# 호출 방식

```java
@RestController
class ExampleController{
	private final ChatClient chatClient;
	
	public ExampleController(Builder chatClientBuilder) {
		this.chatClient = chatClientBuilder.build();
	}
	
	@GetMapping("/ai")
	String generation(String userInput) {
		return this.chatClient.prompt()
							.user(userInput)
							.call()
							.content();
	}
}
```

- `ChatClient.Builder` 는 **프로토타입 스코프 빈**이다.
    - 따라서, `Controller` , `Service` 같은 레이어에서 필드로 `ChatClient.Builder` 를 가지고 있으면 안된다.
    - 빌더는 가변(mutable) 객체이기 때문이다.
        - 만약 A 스레드가 서비스 Builder의 특정값을 바꾸면, 동시에 일을 처리하는 다른 스레드에도 해당 영향을 받게되는 **동시성 문제가 발생**한다.
    - 프로토타입을 사용하면 서로 다른 빈이 같은 빌더를 사용할 때 서로 오염시키지 않는다.

    ```java
    @Bean
    ChatClient a(ChatClient.Builder builder) { ... } // 빌더 인스턴스 1 주입
    
    @Bean
    ChatClient b(ChatClient.Builder builder) { ... } // 빌더 인스턴스 2 주입
    ```


<aside>
💡

#### 프로토타입 스코프 빈

- 컨테이너에서 꺼낼 때마다 새로 만든다.
    - 싱글톤처럼 컨테이너가 인스턴스를 캐시해두지 않는다.
- 컨테이너가 생명주기를 끝까지 책임지지 않는다.
    - 인스턴스 생성, 의존성 주입, 초기화 콜백까지만 관여한다.
- 지연 생성
    - 싱글톤은 컨테이너 기동 시점에 만들어지지만, 프로토타입은 실제로 요청될 때 만들어진다.
- 싱글톤에 주입하면 한 번만 주입된다.
    - 싱글톤 인스턴스에 프로토타입이 주입되는 건 싱글톤 인스턴스가 만들어질 때만 주입된다.
    - 즉, 싱글톤은 한 번 주입된 프로토타입 인스턴스를 계속 사용한다.
</aside>