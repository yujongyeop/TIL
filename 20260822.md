<aside>
💡
java.util.function 패키지
일반적으로 자주 쓰이는 형식의 메소드를 함수형 인터페이스로 미리 정의해 놓은 패키지이다. 재사용성과 유지보수 측면에서 매번 새로운 함수형 인터페이스를 정의하는 것보다 이 패키지의 인터페이스를 활용하는 것이 좋다.
</aside>

# 기본적인 함수형 인터페이스

- 매개변수와 반환값의 유무에 따라 4개의 함수형 인터페이스가 정의되어 있음
- `Predicate`: boolean 반환인 점을 제외하면 `Function`과 동일

| 인터페이스 | 메소드 | 설명 |
| --- | --- | --- |
| Runnable | `void run()` | 매개변수(x) 반환값(x) |
| Supplier\<T\> | `T get()` | 매개변수(x) 반환값(o) |
| Consumer\<T\> | `void accept(T t)` | 매개변수(o) 반환값(x) |
| Function\<T,R\> | `R apply(T t)` | 하나의 매개변수를 받아서 결과를 반환 |
| Predicate\<T\> | `boolean test(T t)` | 조건식을 표현하는데 사용 |

- T: Type, R: Return type
- 매개변수가 두개일 경우: 인터페이스 앞에 `Bi` 접두사 추가
    - 두개 초과 시 직접 정의 필요

<aside>
💡
`UnaryOperator`와 `BinaryOperator`
Function의 또다른 변형으로, 매개변수 타입과 반환타입이 모두 일치한다는 점을 제외하면 Function과 동일하다.
</aside>

# 기본형을 사용하는 함수형 인터페이스

- 기본형 처리를 위해 래퍼 클래스를 사용하는 것은 비효율
- Java에서 효율적인 처리를 할 수 있도록 기본형을 사용하는 함수형 인터페이스를 제공

| 인터페이스 | 메소드 | 설명 |
| --- | --- | --- |
| A**To**B**Function** | `int applyAsInt(double d)` | 입력: A타입, 출력: B타입 |
| **To**B**Function**\<T\> | `int applyAsInt(T value)` | 입력: 제네릭, 출력: B타입 |
| A**Function**\<R\> | `R apply(T t, U u)` | 입력: A타입, 출력: 제네릭 |
| **Obj**Int**Consumer**\<T\> | `void accept(T t, U u)` | 입력: T타입 및 A타입, 출력: 없음 |

# 메소드 참조

- 하나의 메소드만 호출하는 람다식을 `클래스이름::메소드이름` 또는 `참조변수::메소드이름`으로 표현

| 종류 | 람다 | 메소드 참조 |
| --- | --- | --- |
| static메소드 참조 | (x) → ClassName.method(x) | ClassName::method |
| 인스턴스메소드 참조 | (obj, x) → obj.method(x) | ClassName::method |
| 특정 객체 인스턴스메소드 참조 | (x) → obj.method(x) | obj::method |

- 생성자를 호출하는 람다식도 메소드 참조로 변환 가능

```java
() -> new MyClass(); ==> MyClass::new;
(i) -> new MyClass(i); ==> MyClass::new;
x -> new int[x]; ==> int[]::new;
```