# 제네릭 와일드카드 - `?`

- 제네릭 공부를 시작하게 된 계기이다..

> 💡 **공변과 무공변**
>
> 배열은 공변(covariant)이라 `String[]`을 `Object[]`에 넣을 수 있다.
> 제네릭은 무공변(invariant)이라 `List<String>`을 `List<Object>`에 넣을 수 없다.
>
> ```java
> Object[] arr = new String[3];
> arr[0] = 42;               // 컴파일 OK, 런타임에 ArrayStoreException 💥
>
> List<Object> list = new ArrayList<String>();  // 애초에 컴파일 에러 ✅
> ```

- 제네릭은 무공변을 채택해서 컴파일 환경에서 에러가 발생하여 안정성을 얻었지만, 그 대신 유연성을 잃었다.
- 이 유연성을 보완하기 위한 장치가 와일드카드(`?`)이다.

| 형태 | 의미 | 읽기 | 쓰기 |
| --- | --- | --- | --- |
| `List<?>` | 뭔지 모르는 타입 | `Object`로만 가능 | `null`만 |
| `List<? extends T>` | `T`이거나 `T의 하위` | `T`로만 가능 | 불가 |
| `List<? super T>` | `T`이거나 `T의 상위` | `Object`로만 가능 | `T`와 그 하위 |

## `? extends` - 읽기 전용

- 위 표에서 알 수 있듯 `<? extends T>`를 할 경우 **쓰기가 불가능**하다.
- `<? extends T>`는 "T거나 T의 하위 타입 중 하나인 것을 보장하지만, **그게 어떤 타입인지는 모른다.**" 의미이다.
    - 예를 들어 `<? extends Number>`이면 `Number`이거나 `Number의 하위 구현체` 중 하나인 것은 보장하지만, 그것이 `Integer, Double, Long…` 인지는 알 수 없다는 의미이다.

```java
List<? extends Number> list = new ArrayList<Integer>();
list.add(3);   // 컴파일 시 에러가 발생한다.
```

- 값을 넣는 걸 허용하게 된다면? `List<? extends Number>`
    - 실제는 `List<Double>`일 때, `.add(3)`를 넣을 수 없다.
    - 실제는 `List<Integer>`일 때, `.add(3.14)`를 넣을 수 없다.
    - 실제는 `List<Integer>`일 때, `Number`를 넣을 수 없다.

## `? super` - 쓰기 가능

- `<? extends T>` 마찬가지로 어떤 타입인지 모르는 건 동일하다.
- `extends`와 다르게 `super`는 아래쪽 하한을 둔다.
    - `? super Integer`일 때 읽기 시 후보가 될 수 있는 타입들
      `Integer` → `Number` → `Comparable<Integer>` → `Serializable` → `Object`
    - `? super Integer`일 때 넣을 수 있는 타입: `Integer`나 `Integer 하위 구현체`
- 후보가 적어지면 넣을 수 있는 값이 많아진다.
    - `List<? super Integer>` - `Integer` 이하만 삽입 가능 → `Integer` 삽입 가능
    - `List<? super Number>` - `Number` 이하만 삽입 가능 → `Integer, Double, Long...` 삽입 가능
- 값을 넣는 것이 가능한 이유 - `List<? super Number>`
    - 실제는 `List<Number>`일 때, `.add(3)` or `.add(3.14)`를 할 수 있다.
    - 실제는 `List<Object>`일 때, `.add(3)` or `.add(3.14)`를 할 수 있다.
    - 실제는 `List<Number>`일 때, `Number` 타입을 삽입할 수 있다.

> 💡 **Q. 읽기를 `Object`로만 가능한 이유**
>
> A. `super`도 `extends`와 마찬가지로 어떤 타입인지는 모른다. 하지만 `Object`는 모든 객체의 조상이기 때문에 어떠한 `? super T` 상황에서도 후보군에 들어간다. 또한, `Object`의 특성상 모든 값을 받을 수 있기 때문에 안전하게 값을 받을 수 있다.