# 구조화된 출력과 스트리밍
# LLM 반환값

- Chat Model의 출력은 텍스트 덩어리이다.
- `.content()` 를 호출하면 `String` 을 반환한다.
    - 자바 코드 입장에서는 응답을 사용하기 힘들다.
- 결과를 사용하기 위해서는 문자열을 특정 객체로 바꿔야한다.
    - 하지만 LLM이 반환한 응답으로는 해당 작업을 하기 어렵다.
    - 프롬프트에 json 형식을 주고 해당 형식으로 반환할 것을 요구한다.
        - 이렇게 할 경우 `ObjectMapper` 로 파싱이 잘 될 때도 있지만, 안 될 때도 있다.
        - 이는 LLM이 블랙박스 구조이기 때문이다.

          `.user("톰 행크스의 필모그래피를 JSON으로 줘. 형식: {\"actor\": \"...\", \"movies\": [...]}")`


#### 예시

코드

```java
String result = chatClient.prompt()
									.user("톰 행크스의 필모그래피 알려줘")
									.call()
									.content();
```

반환

```
톰 행크스는 정말 많은 명작에 출연했습니다! 대표작으로는 포레스트 검프, 캐스트 어웨이, 라이언 일병 구하기 등이 있어요.
```

# `Structured Output`

- **모델의 텍스트 응답을 지정한 타입의 자바 객체로 받는 기능이다.**
- `.content()` 대신 `.entity()` 를 사용하면 된다.

```java
ActorsFilms films = chatClient.prompt()
        .user("아무 배우 한 명의 필모그래피를 생성해줘.")
        .call()
        .entity(ActorsFilms.class);

films.actor();   // "Tom Hanks"
films.movies();  // ["Forrest Gump", "Cast Away", ...]
```

- **Spring AI가 개발자가 손으로 하던 프롬프트 엔지니어링을 자바 타입으로부터 자동 생성해준다.**
    - AI 의존적인 기술이기 때문에 100%를 보장하지 않는다.
- Claude, ChatGPT, Google 같은 프론티어 모델은 이 방식이 꽤 잘 통하지만, 로컬에서 돌리는 작은 오픈소스 모델에서는 잘 안 된다.
- `.entity()` 는 `.call()` 전용이다.
    - 스트리밍(`.stream()`)에서는 쓸 수 없다.
    - 제네릭 타입은 `List<ABC>.class` 가 자바에서 불가능하기 때문에, `ParameterizedTypeReference<List<ABC>>(){}` 같이 사용해야한다.

```java
List<ActorsFilms> list = chatClient.prompt()
        .user("배우 3명의 필모그래피를 생성해줘.")
        .call()
        .entity(new ParameterizedTypeReference<List<ActorsFilms>>() {});
```

## 동작 과정

1. 타입 → JSON Schema 생성한다.
2. 스키마를 프롬프트 뒤에 자동으로 덧붙인다.
3. 모델 응답 문자열을 파싱해서 객체로 변환한다.
    - `{"actor":"Tom Hanks",...}` → 엔티티 인스턴스

## 관련 메소드

- `useProviderStructuredOutput()` - 부탁하는 대신 프로바이더의 네이티브 API로 생성 자체를 강제한다.
- `validateSchema()` - 응답을 스키마로 검증하고, 실패하면 구체적 오류를 프롬프트에 붙여 자동 재시도를 한다.
- 두 기능 모두 기본값은 비활성화이다.

# Streaming

- `.call()` 은 모델이 응답을 다 만들 때까지 기다렸다가 한 번에 반환한다.
    - 긴 답변이면 사용자는 5~10초를 빈 화면을 보면서 기다려야 한다.
- 실시간으로 생성되는 답변을 바로 보고 싶다면 `.stream()` 을 사용하면 된다.
    - 웹으로 LLM 쓸 때 타이핑하는 느낌으로 보여줄 수 있다.
- `String` 대신 `Flux<String>` 을 반환한다.
    - 모델이 토큰을 뱉는 족족 청크가 나오는 스트림이다.
    - 컨트롤러는 `SSE` 로 데이터를 내보내면 된다.

## Stream과 Tool Calling

- Spring AI 1.x 버전에서는 `ChatModel` 구현체 안에 call용/stream용 툴 실행 루프가 들어 있었다.
- Spring AI 2.0으로 넘어오면서 내장 툴 실행 루프를 ChatModel에서 걷어내고, ToolCallingAdvisor가 그 역할을 대신하는 것으로 바뀌었다.
- 1.x 버전에서는 중간단계관찰이 `private`메소드이고, `훅(Hook)` 이 존재하지 않아 불가능하였지만, 2.0 버전부터는 `public` 메소드로 변경 및 `훅(Hook)` 추가되어 가능해졌다.