# Advisor 이해 전 문제 사항

- LLM에 질문을 보낸다고 했을 때, 사용자의 **입력을 그대로 LLM에 보내면 안 된다.**

```text
[시스템] 너는 고객 지원 담당자다.
[이전 대화] 사용자: 환불 문의드려요 / AI: 주문번호를 알려주세요...
[검색된 문서] 환불 정책 3조: 구매 후 7일 이내...
[사용자] 지난번에 말한 그거 어떻게 됐어?
```

- 예시와 같이 사용자가 "지난번에 말한 그거 어떻게 됐어?"라는 질문 앞에는 **덧붙어야 할 것들이 있다**.
    - 응답을 대화 기록에 저장하고, 로그를 남기고, 모델이 툴을 쓰겠다고 할 경우 툴을 실행해서 다시 물어봐야 한다.
- 이런 작업은 사용자 질문 앞뒤로 붙어야 한다.

```text
요청 →  [메모리]  →  [RAG]  →  [로깅]  →  LLM
응답 ←  [메모리]  ←  [RAG]  ←  [로깅]  ←
```

# Advisor 체인

- 위 같은 작업은 Spring Security Filter 체인과 같이 Advisor를 체인 형식으로 엮어놓은 것을 의미한다.
    - Security의 `doFilter()` 메소드와 비슷한 `nextCall()` 메소드가 있다.
- 다만, Security Filter 체인은 각 필터를 한 번만 통과하지만, Advisor 체인은 **반복해서 통과시킬 수 있다**. (Spring AI 2.0부터)

## Advisor

- LLM 호출 앞뒤에 끼워 넣는, 재사용 가능한 처리 단계를 의미한다.

### 할 수 있는 일

- **통과시키며 관찰하기** - 요청과 응답을 들여다보고 로그만 남긴다. 아무것도 바꾸지 않는다.
- **변형하기** - 요청에 시스템 메시지나 검색된 문서를 덧붙이거나, 응답에서 민감정보를 지운다. `메모리`와 `RAG Advisor`가 여기에 속한다.
- **차단하기** - 다음으로 넘기지 않고 자기가 응답을 만들어 되돌려 보낸다. 캐시 히트나 금지어 필터가 여기에 해당하며, 이 경우 **LLM 호출 자체가 일어나지 않는다.**
- **반복하기** - 아래 구간을 여러 번 돌린다. (2.0 신기능)
    - `ToolCallingAdvisor`보다 뒤에 있는 어드바이저들은 **매 반복마다 실행**된다.

## Advisor가 Spring AI의 핵심인 이유

Spring AI의 기능들이 사실상 **전부 어드바이저**이다.

| 기능 | 정체 |
| --- | --- |
| 대화 메모리 | Advisor |
| RAG | Advisor |
| 툴 호출 | Advisor (재귀) |
| 구조화 출력 검증 | Advisor (재귀) |
| 유해 콘텐츠 차단 | Advisor |
| 로깅 | Advisor |

# Advisor 인터페이스

```java
public interface Advisor extends Ordered {
    String getName();
}

public interface CallAdvisor extends Advisor {
    ChatClientResponse adviseCall(
        ChatClientRequest chatClientRequest, CallAdvisorChain callAdvisorChain);
}

public interface StreamAdvisor extends Advisor {
    Flux<ChatClientResponse> adviseStream(
        ChatClientRequest chatClientRequest, StreamAdvisorChain streamAdvisorChain);
}
```

- 호출 기반 Advisor = `CallAdvisor`
- Stream 기반 Advisor = `StreamAdvisor`
- `ChatClientRequest` - 아직 모델로 나가지 않아 어드바이저가 고쳐 쓸 수 있는 요청 객체 (`Prompt` + `Context`)
- `ChatClientResponse` - 모델이 돌려준 응답에 그 `Context`를 함께 실어 체인을 거슬러 올라가는 객체 (`ChatResponse` + `Context`)

> **💡 Context란?**
>
> - `Map<String, Object>`만 가지고 있는 객체이다.
> - 같은 어드바이저의 전/후 사이 전달을 통해 응답 처리 소요 시간을 계산할 수 있도록 값을 저장한다.
> - 어드바이저끼리 전달되어 앞선 어드바이저가 판단한 결과를 뒤에서 참고하는 경우가 있다.