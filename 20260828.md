# 자바 제네릭

- 클래스 내부에서 사용할 데이터 타입을 외부에서 지정하는 기법
- 객체별로 다른 타입의 자료가 저장될 수 있도록 함
- `<>` 안에 들어가는 타입이 제네릭

```java
ArrayList<String> list = new ArrayList<>();
```

- `Collection<T>` 에서 `T` 를 타입 매개변수라 함

## 타입 파라미터 정의

- 타입 매개변수는 제네릭을 이용한 클래스나 메소드를 설계할 때 사용
    - 위 예시 코드처럼 쓸 수 있는 클래스나 메소드를 설계할 때 사용한다는 의미로 추정

```java
class Box<T> {
	List<T> values = new ArrayList<>();

	public void add(T value){
		values.add(value);
	}
}
```

- 위 코드처럼 선언한 클래스는 다음과 같이 사용 가능

```java
Box<Integer> intBox = new Box<>();
Box<String> stringBox = new Box<>();
Box<Custom> customBox = new Box<>();
```

> 💡 **타입 파라미터 생략**
> JDK 1.7 버전부터 new 생성자 부분의 제네릭 타입을 생략할 수 있게 되었다.

> 💡 **타입 파라미터 할당 가능 타입**
> 제네릭에서 할당받을 수 있는 타입은 Reference 타입뿐이며, 자바 기본(Primitive) 타입은 사용 불가

## 제네릭 장점

### 1. 컴파일 단계에서 타입 검사를 통한 예외 방지

- JDK 1.5 이전 버전에서는 여러 타입을 다루기 위해 `Object`를 사용해야 했고, 이로 인해 반환된 객체를 다시 원하는 타입으로 변환해야 하며, 런타임(CCE) 에러가 발생할 가능성도 존재했음

### 2. `Object`를 사용했을 때보다 성능 향상

- `Object`를 원하는 타입으로 다운 캐스팅할 때 오버헤드가 발생하는데, 제네릭 사용 시 다운 캐스팅을 할 필요가 없음

## 제네릭 유의 사항

### 1. 제네릭 타입의 객체는 생성할 수 없음

```java
T t = new T(); //불가
```

### 2. `static` 멤버에 제네릭 타입을 사용할 수 없음

```java
class Box<T> {
	public static T add(int n); // 불가
	public static void add(T n); // 불가
}
```